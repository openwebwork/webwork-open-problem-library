# DESCRIPTION
# AP Question for Limits
# ENDDESCRIPTION

## DBsubject("Calculus")
## DBchapter("Derivatives")
## DBsection("Derivative Rules")
## KEYWORDS("derivative")

# Init

DOCUMENT();

loadMacros(

	"PGstandard.pl",
	"MathObjects.pl",
	"PGchoicemacros.pl"

);

# Setup

Context("Numeric");

$radio = new_multiple_choice();

$radio -> qa(

	"Ig \( f \) and \( g \) are twice differentiable and if \( h(x)=f(g(x)) \), then \( h''(x)= \)",
	"\( f''(g(x))[g'(x)]^{2}+f'(g(x))g''(x) \)"

);

$radio -> extra(

	"\( f''(g(x))g'(x)+f'(g(x))g''(x) \)",
	"\( f''(g(x))[g'(x)]^{2} \)",
	"\( f''(g(x))g''(x) \)",
	"\( f''(g(x)) \)"

);

# Main Text

TEXT(beginproblem());
Context() -> texStrings;

BEGIN_TEXT

\{ $radio -> print_q() \}
\{ $radio -> print_a() \}

END_TEXT

Context() -> normalStrings;

# Answer

ANS(radio_cmp($radio -> correct_ans()));

ENDDOCUMENT();
