##BMCC MAT302

########################################################################

DOCUMENT();      

loadMacros(
   "PGstandard.pl",     # Standard macros for PG language
   "MathObjects.pl",
   #"source.pl",        # allows code to be displayed on certain sites.
   #"PGcourse.pl",      # Customization file for the course
);

# Print problem number and point value (weight) for the problem
TEXT(beginproblem());

# Show which answers are correct and which ones are incorrect
$showPartialCorrectAnswers = 1;

##############################################################
#
#  Setup
#
#
Context("Numeric");

$y = random(0);
$x = random(2,5,1);
$line = random(4,6,1);

$f = Formula("pi*x**7/7");
$ans1 = $f->eval(x=>"$x") - $f->eval(x=>"0");

$g = Formula("2*pi*x**5/5");
$ans2 = $g->eval(x=>"$x") - $g->eval(x=>"0");


##############################################################
#
#  Text
#
#

Context()->texStrings;
BEGIN_TEXT

Use the disk method or the shell method to find the volumes of the solids generated by revolving the region bounded by the graphs of the equations about the given lines.

$PAR
\(y = x^3\), \(\quad y = $y\), \(\quad x = $x\)
$PAR
a) the \(x\)-axis
$BR
volume = \{ans_rule(10)\} cubic units.
$PAR
b) the \(y\)-axis
$BR
volume = \{ans_rule(10)\} cubic units.
$PAR
END_TEXT
Context()->normalStrings;

ANS(num_cmp($ans1));
ANS(num_cmp($ans2));

ENDDOCUMENT();        
