##DESCRIPTION
##  Algebra problem: Find the values of a, h, k, and find the vertex given the equation of a parabola in standard (vertex) form.
##ENDDESCRIPTION

##KEYWORDS('algebra', ‘parabola’, 'quadratic function’, ‘vertex’)

## DBsubject(Algebra)
## DBchapter(Quadratic functions)
## DBsection(Parabolas)
## Level(2)
## Author(Sybil Shaver)
## Institution(CUNY City Tech)
## Language(en)

########################################################################

DOCUMENT();      

loadMacros(
   "PGstandard.pl",     # Standard macros for PG language
   "MathObjects.pl",
   "parserPopUp.pl"
);

# Print problem number and point value (weight) for the problem
TEXT(beginproblem());

# Show which answers are correct and which ones are incorrect
$showPartialCorrectAnswers = 1;

##############################################################
#
#  Setup
#
#

Context("Numeric");
Context()->flags->set(
   reduceConstants => 0,
   reduceConstantFunctions => 0
);
Parser::Number::NoDecimals;
Context()->noreduce('(-x)+y', '(-x)-y');

$a = 1;
$h = non_zero_random(-9,9,1);
$k =  non_zero_random(-9,9,1);
##############################################################
#
#  Text
#
#

Context()->texStrings;
BEGIN_TEXT

$PAR
For the parabola which is the graph of the function \( y = $a(x-$h)^2 +$k \), identify the parameters $a, $h, and $k, and find the vertex of the parabola.
$PAR
$BBOLD Step 1: $EBOLD Identify the parameters a, h, and k:
$BR
a: \{ ans_rule(5) \}
$BR 
h: \{ ans_rule(5) \}
$BR
k: \{ ans_rule(5) \}
$PAR
$BBOLD Step 2: $EBOLD Find the vertex: \{ ans_rule(5) \}

$BR
Note: Your answers must be in exact form, do not give their decimal values.
$BR
Use "sqrt()" to enter square roots. For example, "sqrt(6)" would represent \(\sqrt{6}\).

END_TEXT
Context()->normalStrings;

##############################################################
#
#  Answers
#
#

ANS(Real($a)->cmp);
ANS(Real($h)->cmp);
ANS(Real($k)->cmp);

COMMENT(“Programmed by editing a vaguely related problem written by K. Andrew Parker”);

ENDDOCUMENT();        
