## DESCRIPTION
## DBsubject('Introduction to Statistics')
## DBchapter('Part II From Exploration to Inference')
## DBsection('Chapter 11: Sampling Distributions')
## KEYWORDS('statistic', 'parameter', 'sampling distribution', 'sample')
## TitleText1('The Basic Practice of Statistics')
## EditionText1('5e')
## AuthorText1('David Moore')
## Section1('Chapter 11: Sampling Distributions')
## Problem1('')
## Author('R Cruz, L Danielson, J Guild')
## Institution('The College of Idaho')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
  "PGstandard.pl",
  "MathObjects.pl",
  "answerHints.pl"
  );

TEXT(beginproblem());

######################################
#  Setup

$x1 = random(2, 5, 1);
@x = ($x1, $x1 + random(1, 2, 1), $x1 + random(3, 5, 1), $x1 + random(6, 8, 1));
@xBar = (($x[0]+$x[1])/2, ($x[0]+$x[2])/2, ($x[0]+$x[3])/2, ($x[1]+$x[2])/2, ($x[1]+$x[3])/2, ($x[2]+$x[3])/2);

$mu = ($x[0] + $x[1] + $x[2] + $x[3]) / 4;

######################################
#  Main text

BEGIN_TEXT
Consider the population of four juvenile condors.  Their weights in pounds are:
\($x[0],\) \($x[1],\) \($x[2],\) \($x[3]\)
$PAR
(a)    Let \( x \) be the weight of a juvenile condor selected from this population. 
$BR $SPACE $SPACE
Write the possible values for \(x\): \{ans_rule(30)\}
$BR $SPACE $SPACE
${BITALIC}NOTE:  Separate each value in the list with a comma.$EITALIC
$PAR
(b)   Find the mean of the population: \{ans_rule(10)\} 
$BR
$PAR
(c)     Let \( \bar x \) be the average weight from a sample of two juvenile condors. 
$BR $SPACE $SPACE 
List all possible outcomes for \( \bar x \):  \{ans_rule(30)\}
$BR $SPACE $SPACE
This is the sampling distribution for samples of size \(2.\) 
$BR $SPACE $SPACE
${BITALIC}NOTE: If a value occurs twice, make sure to list it twice. Separate each value in the list with a comma.$EITALIC
$PAR
(d)     Find the mean of the sampling distribution: \{ans_rule(10)\} 
$BR
END_TEXT

######################################
#  Answers

$showPartialCorrectAnswers = 1;

Context()->flags->set(tolerance=>0.001, tolType=> 'absolute');

$ans_a = List("$x[0], $x[1], $x[2], $x[3]");
ANS($ans_a->cmp);

$ans_b = Compute($mu);
ANS($ans_b->cmp->withPostFilter(AnswerHints(
     sub {
         my ($correct,$student,$anshash) = @_;
         return abs($student-$correct).01;
         } => ["Your answer is close.  Use more accuracy in your calculation."])));

$ans_c = List("$xBar[0], $xBar[1], $xBar[2], $xBar[3], $xBar[4], $xBar[5]");
ANS($ans_c->cmp);

$ans_d = Compute($mu);
ANS($ans_d->cmp->withPostFilter(AnswerHints(
     sub {
         my ($correct,$student,$anshash) = @_;
         return abs($student-$correct)<0.01;
         } => ["Close!"])));

######################################
#  Solution

BEGIN_SOLUTION
$BR 
(a) The possible weight of a juvenile condor selected from this population: \($ans_a\)
$BR
(b) Calculate the mean of the population:  \(\dfrac{$x[0] + $x[1] + $x[2] + $x[3]}{4} = $ans_b\)
$BR $BR
(c) The values for the sampling distribution for samples of size \(2.\): \($ans_c\)
$BR
(d)The mean of the sampling distribution is the same as the population: \($ans_b\)
$BR $SPACE $SPACE
To see this calculate:
\[\frac{$xBar[0]+$xBar[1]+$xBar[2]+$xBar[3]+$xBar[4]+$xBar[5]}{6} = $ans_d\]
END_SOLUTION

ENDDOCUMENT();       # This should be the last executable line in the problem.










