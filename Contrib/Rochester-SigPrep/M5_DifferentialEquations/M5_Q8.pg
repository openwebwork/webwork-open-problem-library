##DESCRIPTION
##  Problem Set-5: Differential Equations , differential equation for R-L-C circuit.
##ENDDESCRIPTION

## DBsubject('Differential equations')
## DBchapter('Higher order differential equations ')
## DBsection('Applications')
## Author('Utku Demir, Gaurav Sharma')
## Institution('University of Rochester')
## KEYWORDS('Differential','equation', 'circuit')
## Level('')

########################################################################

DOCUMENT();      

loadMacros(
"PGstandard.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"parserFunction.pl",
"answerHints.pl",
"PGcourse.pl",
);


# Show which answers are correct and which ones are incorrect
$showPartialCorrectAnswers = 1;



#############################
#  Setup

Context("Complex");
Context()->variables->are(
w=>"Real",
t=>"Real",
a=>"Real",
b=>"Real",
I=>"Real",
R=>"Real",
C=>"Real",
d=>"Real",
V=>"Real",
L=>"Real"
);



# Redefine the Heaviside function so the answer checker will take either 1 or #h(t).
#parserFunction("h(t)" => "1");
# skip above and use step - need to assign it
## required to use step function
Context()->functions->add(
  step => {
    class => 'Parser::Legacy::Numeric',
    perl => 'Parser::Legacy::Numeric::do_step'
  },
);

Context()->flags->set(
  tolerance => 0.001,
  tolType => "absolute",
);


$f3 = Formula(" d^2I(t)/(dt^2) + R/LdI(t)/(dt) + 1/(LC)I(t) ");

BEGIN_TEXT
$BBOLD Differential equation for R-L-C circuit $EBOLD
$PAR
Based on the videos you have watched you should be able to answer this question without watching another video. 
$PAR
\{ image( "RLC2.png", width=>150, height=>150,  
tex_size=>700, extra_html_tags=>'alt="Graph of a function."' ) \}

$PAR
The switch is closed at \( t = 0 \), when the capacitor is fully charged having the voltage value of \( V_0 \). The resistance, the inductance, and the capacitance of the resistor, the inductor and the capacitor are \( R \), \( L \), and \( C \), respectively. Please enter the differential equation that represents the current in the circuit \( I(t) \) in terms of \( R \), \( L \), \( C \) and time \( t \).
$PAR
$BBOLD Hint-1: Your answer will be second order linear constant coefficient differential equation $EBOLD
$PAR
$PAR
$BBOLD
Hint-2: The coefficient of the highest order differential term will be 1
$EBOLD
$PAR
\{ ans_rule(20)\} = \( 0 \)
$PAR

END_TEXT
Context()->normalStrings;

##############################################################
#
#  Answers
#
#

ANS( $f3-> cmp());
# relative tolerance --3.1412 is incorrect but 3.1413 is correct
# default tolerance is .01 or one percent.


ENDDOCUMENT();        



