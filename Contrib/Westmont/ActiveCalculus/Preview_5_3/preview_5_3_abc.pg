# DESCRIPTION
# Preview Activity from _Active Calculus_ by Matthew Boelkins
# ENDDESCRIPTION

## DBsubject('Calculus - single variable')
## DBchapter('Integrals')
## DBsection('Antiderivatives')
## KEYWORDS('integral', 'Antiderivatives')
## TitleText1('Active Calculus')
## EditionText1('2015')
## AuthorText1('Matthew Boelkins')
## Section1('5.1')
## Problem1('Preview Activity 5.1abc')
## Author('Branden Stone')
## Institution('Adelphi University')
## RESOURCES('preview_5_1.png')

DOCUMENT();
loadMacros(
"PGstandard.pl",
"MathObjects.pl",
"PGessaymacros.pl",
"PGcourse.pl",
);

Context("Numeric");

$g = Formula("e^(3x)");
$dg = $g->D('x');
$h = Formula("sin(5x+1)");
$dh = $h->D('x');
$p = Formula("arctan(2x)");
$dp = $p->D('x');
$q = Formula("(2-7x)^4");
$dq = $q->D('x');
$r = Formula("3^(4-11x)");
$dr = $r->D('x');

Context()->variables->add(C => 'Real');
$M = Formula("(1/3)e^(3x)+C");
$N = Formula("(1/5)sin(5x+1)+C");
$S = Formula("(1/2)arctan(2x)+C");
$V = Formula("-(1/28)(2-7x)^4+C");
$W = Formula("-3^(4-11x)/(11*ln 3)+C");

$A = Formula("1/pi*sin(pi*x)+C");
$B = Formula("1/4*1/12*(4x+7)^(12)+C");


TEXT(beginproblem());
$showPartialCorrectAnswers = 0;

install_problem_grader(~~&std_problem_grader); #for correct behavior with essay
Context()->texStrings;
BEGIN_TEXT
$PAR
Recall the Chain Rule and how it can be applied to 
find the derivative of a composite function.  In particular, if \(u\) is a differentiable 
function of \(x\), and \(f\) is a differentiable function of \(u(x)\), then
\[ \frac{d}{dx} \left[ f(u(x))  \right] = f'(u(x)) \cdot u'(x). \]
In words, we say that the derivative of a composite function \(c(x) = f(u(x))\), 
where \(f\) is considered the "outer" function and \(u\) the "inner" function, is 
"the derivative of the outer function, evaluated at the inner function, times the 
derivative of the inner function."  
$PAR
(a) For each of the following functions, use the Chain Rule to find the function's derivative.  
Notice the label of each derivative respects the functions name (e.g., the derivative of 
\(g(x)\) should be labeled \(g'(x)\)). 
$PAR
(i) If  \(g(x) = e^{3x}\), then \(g'(x) =\)\{ ans_rule \}
$PAR
(ii) If \(h(x) = \sin(5x+1)\), then \(h'(x) =\)\{ ans_rule \}
$PAR
(iii) If \(p(x) = \arctan(2x)\), then \(p'(x) =\)\{ ans_rule \}
$PAR
(iv) If \(q(x) = (2-7x)^4\), then \(q'(x) =\)\{ ans_rule \}
$PAR
(v) If \(r(x) = 3^{4-11x}\), then \(r'(x) =\)\{ ans_rule \}
$PAR
(b) For each of the following functions, use your work in (a) to help you determine 
the general antiderivative of the function. (Recall that the general antiderivative of a function 
includes ``\(+C\)'' to reflect the entire family of functions that share the same derivative.) 
Notice the label of each antiderivative respects the name (e.g., the antiderivative of \(m\) 
should be called \(M\)).  In addition, check your work by computing the derivative of 
each proposed antiderivative.
$PAR
(i) If \(m(x) = e^{3x}\), then \(M(x) =\)\{ ans_rule \}
$PAR
(ii) If \(n(x) = \cos(5x+1)\), then \(N(x) =\)\{ ans_rule \}
$PAR
(iii) If \(\displaystyle s(x) = \frac{1}{1+4x^2}\), then \(S(x) =\)\{ ans_rule \}
$PAR
(iv) If \(v(x) = (2-7x)^3\), then \(V(x) =\)\{ ans_rule \}
$PAR
(v) If \(w(x) = 3^{4-11x}\), then \(W(x) =\)\{ ans_rule \}
$PAR
(c) Based on your experience in parts (a) and (b), conjecture an antiderivative for each 
of the following functions.  Test your conjectures by computing the derivative of each 
proposed antiderivative.
$PAR
(i) \(a(x) = \cos(\pi x)\), then \(A(x)=\)\{ essay_box(1,20) \}
$PAR
(ii) \(b(x) = (4x+7)^{11}\), then \(B(x)=\)\{ essay_box(1,20) \}
$PAR
(iii) \(c(x) = xe^{x^2}\), then \(C(x)=\)\{ essay_box(1,20) \}
$PAR

END_TEXT
Context()->normalStrings;

ANS( Compute($dg) -> cmp );
ANS( Compute($dh) -> cmp );
ANS( Compute($dp) -> cmp );
ANS( Compute($dq) -> cmp );
ANS( Compute($dr) -> cmp );
ANS( Compute($M) -> cmp );
ANS( Compute($N) -> cmp );
ANS( Compute($S) -> cmp );
ANS( Compute($V) -> cmp );
ANS( Compute($W) -> cmp );
ANS( essay_cmp() );
ANS( essay_cmp() );
ANS( essay_cmp() );





Context()->texStrings;
SOLUTION(EV3(<<'END_SOLUTION'));
$PAR SOLUTION $PAR
This needs to be written up.
$PAR
END_SOLUTION
Context()->normalStrings;

ENDDOCUMENT();


