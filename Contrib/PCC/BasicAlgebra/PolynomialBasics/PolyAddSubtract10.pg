# WeBWorK problem written by Chris Hughes
# Portland Community College
#
# Template:
# Is the following a monomial, binomial, or trinomial?
#
#    a x^n + b x^m + c x^k
#
# a, b, c can be any non-zero integer on [-20,20], and n, m are 
# integers on [1,20]. We always choose k<m<n.
#
# Last updated: Carl Yao, 6/28/13
# ENDDESCRIPTION

## DBsubject('Algebra')
## DBchapter('Polynomial and Rational Functions')
## DBsection('Polynomial Functions')
## KEYWORDS('polynomial','exponent','trinomial')
## DBCCSS('A-APR.1')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')
## Author('Alex Jordan, Carl Yao, Chris Hughes')
## Institution('PCC')

##############################################

DOCUMENT();

loadMacros(
  "PGstandard.pl",
  "MathObjects.pl",
  "PGML.pl",
  "parserPopUp.pl",
  "PCCmacros.pl",
  "PGcourse.pl",
);


##############################################

Context("Numeric");
Context()->noreduce('(-x)-y','(-x)+y');

$var = RandomVariableName(type=>'variable');
Context()->variables->are($var=>'Real');

# setup the trinomial: a x^n + b x^m + c x^k
$a = non_zero_random(-20,20,1);
$b = non_zero_random(-20,20,1);
$c = non_zero_random(-20,20,1);
$n = non_zero_random(4,20,1);
$m = non_zero_random(2,$n-1,1);
$k = non_zero_random(1,$m-1,1);

# initialize all of the coefficients as 0
foreach my $i (0..$n) {$poly1array[$i] = 0};

# assign the leading coefficient as $a, and then some other 
# coefficient as $b
$poly1array[0] = $a;
$poly1array[$m] = $b;
$poly1array[$k] = $c;

# reduction check
$poly1 = Formula(PolyString(~~@poly1array,var=>$var))->reduce;

$popup = PopUp(["?", "monomial", "binomial", "trinomial"], "trinomial");
Context("LimitedNumeric");


##############################################
TEXT(beginproblem());

BEGIN_PGML

Is the following expression a monomial, binomial, or trinomial?

    [`[$poly1]`]

    [@$popup->menu()@]*

What is the degree of the expression?

    [________________]

END_PGML

ANS( $popup->cmp() );
ANS( Compute($n)->cmp());

##############################################

BEGIN_PGML_SOLUTION

* Since there are *three* terms, this is a *tri*nomial.
* The highest power of [`[$var]`] is [$n], so the degree of the trinomial is [$n].

END_PGML_SOLUTION
##############################################

ENDDOCUMENT();
