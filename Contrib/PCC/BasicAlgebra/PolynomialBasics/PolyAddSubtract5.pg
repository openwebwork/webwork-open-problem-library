# WeBWorK problem written by Chris Hughes
# Portland Community College
#
# Template:
# Is the following a monomial, binomial, or trinomial?
#
#    a 
#
# a can be any non-zero integer on [-20,20]. 
#
# The main point of this problem is to get the students to 
# write degree 0.
#
# Last updated: Carl Yao, 6/28/13
# ENDDESCRIPTION

## DBsubject('Algebra')
## DBchapter('Polynomial and Rational Functions')
## DBsection('Polynomial Functions')
## KEYWORDS('polynomial','exponent','monomial')
## DBCCSS('A-APR.1')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')
## Author('Alex Jordan, Carl Yao, Chris Hughes')
## Institution('PCC')

##############################################

DOCUMENT();

loadMacros(
  "PGstandard.pl",
  "MathObjects.pl",
  "PGML.pl",
  "parserPopUp.pl",
  "PCCmacros.pl",
  "PGcourse.pl",
);


##############################################

Context("Numeric");
Context()->noreduce('(-x)-y','(-x)+y');

$var = RandomVariableName(type=>'variable');
Context()->variables->are($var=>'Real');

# setup the monomial: a 
$a = non_zero_random(-50,50,1);

# the degree is 0
$n = 0;

# assign the leading coefficient as $a
$poly1array[0] = $a;

# reduction check
$poly1 = Formula(PolyString(~~@poly1array,var=>$var))->reduce;

$popup = PopUp(["?", "monomial", "binomial", "trinomial"], "monomial");
Context("LimitedNumeric");


##############################################
TEXT(beginproblem());

BEGIN_PGML

Is the following expression a monomial, binomial, or trinomial?

    [`[$poly1]`]

    [@$popup->menu()@]*

What is the degree of the expression?

    [________________]

END_PGML

ANS( $popup->cmp() );
ANS( Compute($n)->cmp());

##############################################


BEGIN_PGML_SOLUTION

* Since there is only *one* term, this is a monomial.
* The highest power of [`[$var]`] is [$n], so the degree of the monomial is [$n].

END_PGML_SOLUTION
##############################################

ENDDOCUMENT();
