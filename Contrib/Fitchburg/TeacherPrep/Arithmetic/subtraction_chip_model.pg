# DESCRIPTION
# Base-5 subtraction problems to help with the understanding of the standard
# subtraction algorithm.
# ENDDESCRIPTION

## DBsubject('Elementary Education')
## DBchapter('Arithmetic')
## DBsection('Models')
## KEYWORDS('subtraction', 'chip model', 'column algorithm')
## Author('Peter Staab')
## Institution('Fitchburg State University')

DOCUMENT();
loadMacros("PGstandard.pl", "PGML.pl", "PGcourse.pl",);

Context("Numeric");

# no rebundling
$a1_hundreds = random(3, 9);
do { $b1_hundreds = random(1, 6) } until ($b1_hundreds < $a1_hundreds);
$a1_tens = random(3, 9);
do { $b1_tens = random(1, 6) } until ($b1_tens < $a1_tens);
$a1_ones = random(3, 9);
do { $b1_ones = random(1, 6) } until ($b1_ones < $a1_ones);

$a1   = 100 * $a1_hundreds + 10 * $a1_tens + $a1_ones;
$b1   = 100 * $b1_hundreds + 10 * $b1_tens + $b1_ones;
$ans1 = $a1 - $b1;

# rebundle the ones

$a2_hundreds = random(3, 7);
do { $b2_hundreds = random(1, 4) } until ($b2_hundreds < $a2_hundreds);
$a2_tens = random(3, 9);
do { $b2_tens = random(1, 6) } until ($b2_tens < $a2_tens);
$a2_ones = random(1, 6);
do { $b2_ones = random(2, 9) } until ($b2_ones > $a2_ones);

$a2   = 100 * $a2_hundreds + 10 * $a2_tens + $a2_ones;
$b2   = 100 * $b2_hundreds + 10 * $b2_tens + $b2_ones;
$ans2 = $a2 - $b2;

# rebundle both the ones and the tens

$a3_hundreds = random(3, 7);
do { $b3_hundreds = random(1, 5) } until ($b3_hundreds < $a3_hundreds);
$a3_tens = random(1, 6);
do { $b3_tens = random(2, 9) } until ($b3_tens > $a3_tens);
$a3_ones = random(1, 7);
do { $b3_ones = random(2, 9) } until ($b3_ones > $a3_ones);

$a3   = 100 * $a3_hundreds + 10 * $a3_tens + $a3_ones;
$b3   = 100 * $b3_hundreds + 10 * $b3_tens + $b3_ones;
$ans3 = $a3 - $b3;

BEGIN_PGML
For the following subtraction problems:

1. Create a chip model
2. Use the chip model to produce a solution
3. Verify your solution with the column algorithm

a) [`[$a1]-[$b1]=`][________]{$ans1}

b) [`[$a2]-[$b2]=`][________]{$ans2}

c) [`[$a3]-[$b3]=`][________]{$ans3}

END_PGML

BEGIN_PGML_SOLUTION
a) [$ans1]
b) [$ans2]
c) [$ans3]
END_PGML_SOLUTION

ENDDOCUMENT();
