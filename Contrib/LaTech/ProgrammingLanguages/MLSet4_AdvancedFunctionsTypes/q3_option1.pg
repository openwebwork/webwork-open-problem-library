## DESCRIPTION
## Generates ML expressions using options and asks for their types.
## ENDDESCRIPTION

## DBsubject(Programming Languages)
## DBchapter(ML)
## DBsection(Data Structures)
## Date(12/05/2025)
## Institution(Louisiana Tech)
## Author(Jason Terry)
## Level(2)
## KEYWORDS('programming languages','ml','sml','expressions','type','option')


DOCUMENT();

## Load libraries
loadMacros(
"PGstandard.pl",
"PGcourse.pl",
"contextArbitraryString.pl",
"PGML.pl"
);

## Change context to strings answers
Context("ArbitraryString");

## Create custom answer checker for ML types
$typeChecker = sub {
        my ($correct,$student,$ansHash) = @_;  # get correct and student MathObjects
        $correct = $correct->string;
        $student = $student->string;
        $student =~ s/^ *(.*?) *$/$1/;     # remove front/end whitespace
        $student =~ s/ *~~* */~~*/g;       # * shall have no space on each side
        $student =~ s/ *- */-/g;           # - shall have no space on each side
        $student =~ s/ *> */>/g;           # > shall have no space on each side
        $student =~ s/ *' */'/g;           # ' shall have no space on each side        
        $student =~ s/~~( */~~(/g;         # ( shall have no space after
        $student =~ s/ *~~)/~~)/g;         # ) shall have no space before
        $student =~ s/ *list/ list/g;      # list shall have 1 space before
        $student =~ s/ *option/ option/g;  # option shall have 1 space before
        return ($correct eq $student);     # return 1 if correct, 0 otherwise
    };

## Create a random parameter
$n = random(1,9,1);

## Create answers to problems and install custom type checker
$ans1 = String("unit option")->cmp(
        checker => $typeChecker
    );

$ans2 = String("int option option")->cmp(
        checker => $typeChecker
    );

$ans3 = String("'a option option")->cmp(
        checker => $typeChecker
    );

$ans4 = String("int list option")->cmp(
        checker => $typeChecker
    );

$ans5 = String("int option list")->cmp(
        checker => $typeChecker
    );


## Display question
TEXT(beginproblem());

BEGIN_PGML
Write the type of the following ML expressions. *WARNING: Only use required parentheses for types to match the ML compiler's response.*

    *SOME () : [_________________________]{$ans1}*
===
    *SOME (SOME [$n]) : [_________________________]{$ans2}*
===
    *SOME NONE : [_________________________]{$ans3}*
===
    *SOME \[[$n]\] : [_________________________]{$ans4}*
===
    *\[SOME [$n]\] : [_________________________]{$ans5}*


END_PGML

BEGIN_PGML_SOLUTION
Solution:

    *SOME () : unit option*
===
    *SOME (SOME [$n]) : int option option*
===
    *SOME NONE : \'a option option*
===
    *SOME \[[$n]\] : int list option*
===
    *\[SOME [$n]\] : int option list*


END_PGML_SOLUTION

ENDDOCUMENT();
