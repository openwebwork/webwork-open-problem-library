## DESCRIPTION
## College Algebra, find the slope of a line given two points
## ENDDESCRIPTION


## DBsubject(Algebra)
## DBchapter(Linear equations and functions)
## DBsection(Parallel and perpendicular lines)
## Date(06/17/2016)
## Institution(Colorado Community College System)
## Author(Steven Wray)
## MO(1)
## KEYWORDS('college algebra', 'slope')


###########################
#  Initialization

DOCUMENT();

loadMacros(
"PGstandard.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"PGML.pl",
"PGcourse.pl",
"parserRadioButtons.pl",
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

###########################

# Select parallel, perpendicular, or neither
$u=non_zero_random(1,3);

if ($u == 1) {
  #  Setup parameters 
  $a=non_zero_random(2,7);
  $b=non_zero_random(1,3);

  $c=non_zero_random(2,5);
  $d=non_zero_random(2,5);
  $e=non_zero_random(-6,-3);
  $f=non_zero_random(-12,-6);

  # points
  $x1 = - $a * $b + $c;
  $x2 = $c;
  $y1 = $b - $d;
  $y2 =  - $d;

  $x3 = -$a * $b + $f;
  $x4 = $f;
  $y3 = $b + $e;
  $y4 = $e;
  ###########################
  #  Main text

  $radio = RadioButtons(
    ["parallel","perpendicular","neither"],
    "parallel" # correct answer
  );

  BEGIN_PGML
  Find the slope of the line passing between each pair of points, and then determine if the 
  lines are parallel, perpendicular, or neither. 

  [`` \left( {[$x1]}, {[$y1]} \right) \text{ and } \left( {[$x2]}, {[$y2]} \right) ``]
 
[``    \left( {[$x3]} , {[$y3]} \right) \text{ and } \left( {[$x4]}, {[$y4]} \right) ``] 

   [_______________]{$radio}


  END_PGML


  ############################
  #  Solution

  #BEGIN_PGML_SOLUTION
  #Solution explanation goes here.
  #END_PGML_SOLUTION

} elsif ($u == 2) {  # perpendicular lines
  #  Setup parameters 
  $a=non_zero_random(2,7);
  $b=non_zero_random(1,3);

  $c=non_zero_random(2,5);
  $d=non_zero_random(2,5);
  $e=non_zero_random(-6,-3);
  $f=non_zero_random(-12,-6);

  # points
  $x1 = - $a * $b + $c;
  $x2 = $c;
  $y1 = $b - $d;
  $y2 =  - $d;

  $x3 = $b + $e;
  $x4 = $e;
  $y3 = $a * $b + $f;
  $y4 = $f;
  ###########################
  #  Main text

  $radio = RadioButtons(
    ["parallel","perpendicular","neither"],
    "perpendicular" # correct answer
  );

  BEGIN_PGML
  Find the slope of the line passing between each pair of points, and then determine if the 
  lines are parallel, perpendicular, or neither. 

  [`` \left( {[$x1]}, {[$y1]} \right) \text{ and } \left( {[$x2]}, {[$y2]} \right) \\ 
    \left( {[$x3]} , {[$y3]} \right) \text{ and } \left( {[$x4]}, {[$y4]} \right) ``] 

   [_______________]{$radio}

  END_PGML


  ############################
  #  Solution

  #BEGIN_PGML_SOLUTION
  #Solution explanation goes here.
  #END_PGML_SOLUTION
} else { # neither parallel or perpendicular
  #  Setup parameters 
  $a=non_zero_random(2,7);
  $b=non_zero_random(1,3);

  $c=non_zero_random(2,5);
  $d=non_zero_random(2,5);
  $e=non_zero_random(-6,-3);
  $f=non_zero_random(-12,-6);

  # points
  $x1 = - $a * $b + $c;
  $x2 = $c;
  $y1 = $b - $d;
  $y2 =  - $d;

  $x3 = $b + $e;
  $x4 = $e;
  $y3 = -$a * $b + $f;
  $y4 = $f;
  ###########################
  #  Main text

  $radio = RadioButtons(
    ["parallel","perpendicular","neither"],
    "neither" # correct answer
  );

  BEGIN_PGML
  Find the slope of the line passing between each pair of points, and then determine if the 
  lines are parallel, perpendicular, or neither. 

  [`` \left( {[$x1]}, {[$y1]} \right) \text{ and } \left( {[$x2]}, {[$y2]} \right) \\ 
    \left( {[$x3]} , {[$y3]} \right) \text{ and } \left( {[$x4]}, {[$y4]} \right) ``] 

   [_______________]{$radio}


  END_PGML


  ############################
  #  Solution

#  BEGIN_PGML_SOLUTION
# Solution explanation goes here.
#  END_PGML_SOLUTION
}

COMMENT('MathObject version. Uses PGML.');

ENDDOCUMENT();