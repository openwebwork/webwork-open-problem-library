##DESCRIPTION
## 
##ENDDESCRIPTION

## KEYWORDS('')
## RESOURCES('UBC-SM-18-064.png')

## DBsubject()
## DBchapter()
## DBsection()
## Date()
## Author()
## Level()
## Institution('University of British Columbia')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')

##############################################################

DOCUMENT();

loadMacros(   
	"PGstandard.pl",     # Standard macros for PG language
	"MathObjects.pl",
	"PGML.pl",
	"parserPopUp.pl",
	"parserMultiAnswer.pl",
	"parserRadioButtons.pl",
	#"source.pl",        # allows code to be displayed on certain sites.
	#"PGcourse.pl", 	 # Customization file for the course
   	);

##############################################################
#
#  Setup
#
#

Context("Numeric");
Context()->variables->add(F=>"Real");
Context()->variables->add(E=>"Real");
Context()->variables->add(L=>"Real");
Context()->variables->add(I=>"Real");
Context()->variables->add(v=>"Real");
Context()->variables->add(A=>"Real");
#given


#image scale
$imgScale = .2;

#image aspect ratio
$width = $imgScale*1356;
$height = $imgScale*1389;

#computation 
$delta = "sqrt(2) F L /((sqrt(2)+1) A E)";

##############################################################
#
#  PGML 
#
#

BEGIN_PGML

[@ image("UBC-SM-18-064.png", width=>[$width], height=>[$height]) @]*

A pin-jointed truss is assembled to form the square shape of side length [`L`] shown in the diagram. All members have the same Young's modulus [`E`]. Poisson's ratio [`v`], cross-section area [`A`], and second moment of area[`I`]. A diagonal force [`F`] acts at the free corner while there is no temperature change. What is the resulting displacement at that corner? Give your answer in terms of [`L`], [`F`], [`A`], and [`E`].

[`\delta=`] [__________]{"$delta"}


END_PGML

##############################################################

ENDDOCUMENT();