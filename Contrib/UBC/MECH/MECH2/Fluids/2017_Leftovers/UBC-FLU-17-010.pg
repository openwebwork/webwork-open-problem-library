##DESCRIPTION
## 
##ENDDESCRIPTION

## KEYWORDS('222PS3')
## RESOURCES('UBC-FLU-17-010.png')

## DBsubject()
## DBchapter()
## DBsection()
## Date()
## Author()
## Level()
## Institution('University of British Columbia')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')

##############################################################

DOCUMENT();

loadMacros(   
	"PGstandard.pl",     # Standard macros for PG language
	"MathObjects.pl",
	"PGML.pl",
	"parserPopUp.pl",
	"parserMultiAnswer.pl",
	"parserRadioButtons.pl",
	#"source.pl",        # allows code to be displayed on certain sites.
	#"PGcourse.pl", 	 # Customization file for the course
   	);

##############################################################
#
#  Setup
#
#

Context("Numeric");
#given
$A = random(500,2000,5);
$B = random(500,1500,5);
$C = random(10,20,.1);
$D = random(50,100,1);
$rhoAir = 1.1;
$g = 9.81;

#image scale
$imgScale =0.4;

#image aspect ratio
$width = $imgScale*1027;
$height = $imgScale*579;

#computation
$vel = sqrt(($g*$A*4)/($rhoAir*($C**2)*pi));
$flow = $vel*($C**2)*pi/4;
$power = $g*$A*$vel/1000;
$omega = $B*sqrt(($D+$A)/$A);
$powerLoad = $power*((($D+$A)/$A)**1.5)/1000;

#set tolerance
Context()->flags->set(
tolerance=>.01);

##############################################################
#
#  PGML 
#
#

BEGIN_PGML

[@ image( "UBC-FLU-17-010.png", width=>[$width], height=>[$height]) @]*

A [`[$A]`] [`kg`] helcopter with [`[$C]`] [`m`]  diameter horizontal blades can hover at sea level when the blades rotate at [`[$B]`] [`rpm`]. The blades move air directly downward with an average velocity proportional to the its rotational speed. Assume the density of air is [`1.1`] [`kg/m^3`] .  
1. Find the downdraft velocity generated by the helicopter.  
2. Find the volumetric flow rate of the downdraft.
3. Calculate the power required by the helicopter.

Now, the helicopter is loaded with [`[$D]`] [`kg`]  of additional mass.  
4. Determine the minimum rotational speed for the helicopter to maintain altitude.
5. Calculate the power required by the helicopter under load.

[`u=`] [_____]{"$vel"}[`m/s`]  
[`\dot{V}=`] [_____]{"$flow"}[`m^3/s`]  
[`P=`] [_____]{"$power"}[`kW`]  
[`\omega=`] [_____]{"$omega"}[`rpm`]  
[`P_{load}=`] [_____]{"$powerLoad"}[`kW`]  


END_PGML

##############################################################

ENDDOCUMENT();