#  DESCRIPTION

#  Tipo: respuesta corta
#  Decir el exponente a^b

#  Problema WeBWork escrito por Edwin Florez,
#  <edwin.florez (at) upr (dot) edu>

#  Adaptado desde el sistema quiz.uprm.edu del archivo
#  pedro_prebasica/expresiones_con_exponentes_enteros/expresiones_algebraicas_potencias_enteras.db

#  ENDDESCRIPTION

## DBsubject(Prebasica)
## DBchapter(Expresiones algebraicas)
## DBsection(Exponentes)
## Level(1)
## KEYWORDS('exponentes','enteros')
## Author(Edwin Florez)
## Institution(UPRM)
## Language(es)

DOCUMENT();

loadMacros('PGstandard.pl', 'PGML.pl', 'PGcourse.pl');

#Si se aplica el método reduce() al Contexto Formula, no usa estas reglas al
#aplicar las técnicas de reducción a expresiones.
#Context()->noreduce('(-x)-y','(-x)+y');

@vars = ("m", "n", "p", "q", "r", "y", "z", "k", "a", "b");
$prob = random(0, 1);
$base = $prob == 0 ? $vars[ random(0, 9) ] : random(-50, 50);

$exponente = random(-100, 100);
$expresion = "$base^{$exponente}";
$answer    = Compute("$exponente");

#  Text

BEGIN_PGML
El exponente de
[```[$expresion]```]

Contestación: [_]{$answer->cmp(studentsMustReduceFractions => 1)}
END_PGML

BEGIN_PGML_SOLUTION
El exponente es el término al que está elevado [`[$base]`], es decir, [`[$exponente]`]. 
END_PGML_SOLUTION

ENDDOCUMENT();
