#  DESCRIPTION

#  Tipo: respuesta corta
#   Orden de operaciones: dada una exp tipo a^n+b/c, encontrar el resultado.

#  Problema WebWork escrito por Edwin Florez,
#  <edwin.florez (at) upr (dot) edu>

#  Adaptado desde el sistema quiz.uprm.edu del archivo
#  pedro_prebasica/order_de_operaciones_numericas/exprexp_sp.db

#  ENDDESCRIPTION

## DBsubject(Algebra)
## DBchapter(Algebra of real numbers and simplifying expressions)
## DBsection(Evaluating expressions)
## Level(1)
## KEYWORDS('operaciones', 'numeros', 'orden')
## Author(Edwin Florez)
## Institution(UPRM)
## Language(es)

DOCUMENT();

loadMacros(
    'PGstandard.pl',            'PGML.pl',
    'contextLimitedNumeric.pl', 'PGcourse.pl'
);

Context('Numeric');

#construyendo la exp. tipo a^n + b / c
$a = random(1, 5);
$n = random(2, 4);
$c = list_random(2, 3, 5, 7);

$b = $c * list_random(2, 3, 5) * list_random(2, 3, 5);
if (random(0, 1) == 1) {
    $b = -$b;
}

$r = $a**$n + $b / $c;

#usado al presentar la solución
$potencia   = $a**$n;
$division   = $b / $c;
$suma_resta = $potencia + $division;

$operacion = $b > 0 ? 'suma' : 'resta';

Context("LimitedNumeric");

$answer = Compute($r);

BEGIN_PGML
[```
[$a]^{[$n]} + [$b] \div [$c]
```]
Efectúe las operaciones indicadas y obtenga el resultado para la expresión que se muestra arriba.

Contestación: [_]{$answer}{10}

[@  helpLink('number')  @]*
END_PGML

BEGIN_PGML_SOLUTION
*Paso 1:* Realizar la potencia:
[```
[$a]^{[$n]} + [$b] \div [$c] = [$potencia] + [$b]\div [$c]
```]

*Paso 2:* Realizar la división: 
[```
[$potencia] + [$b]\div [$c]= [$potencia]+[$division]
```]

*Paso 3:* Realizar la [$operacion]:
[```
[$potencia]+[$division] = [$suma_resta]
```]

La solución es: [` [$answer] `].
END_PGML_SOLUTION

ENDDOCUMENT();
