## DESCRIPTION
## Calculus
## ENDDESCRIPTION

## Tagged by cmd6a 5/31/06

## DBsubject(Calculus - single variable)
## DBchapter(Applications of integration)
## DBsection(Surfaces of revolution)
## Institution(OSU)
## MLT(AI-SR-setup_fillin)
## MLTleader(1)
## Level(3)
## KEYWORDS('calculus','parametric equation','surface area','revolution','integration')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
  "PGstandard.pl",
  "PGchoicemacros.pl",
  "PGcourse.pl"
);

# No partial credit on this problem, so we say:
install_problem_grader(~~&std_problem_grader);


TEXT(beginproblem());
$showPartialCorrectAnswers = 0;

$a = random(2,6,1);
$b = random(1,17,1);
$n = random(2,5,1);
$c = random(0,3,1);
$d = random(4,7,1);
$pi= 4*atan2(1,1);

$soln = "2*$pi*t*sqrt(1 + ($a*$n*(t**($n-1)) + $b)**2)";

BEGIN_TEXT
To find the surface area of the surface generated by revolving the
curve  defined by the parametric equations
\[ x=$a t^{$n} + $b t, \quad y=t, \quad $c\le t\le $d  \]
around the \(x\)-axis you'd have to compute
\[ \int_a^b f(t) dt \]
where a=\{ ans_rule(10) \},
END_TEXT
ANS(num_cmp($c));
BEGIN_TEXT
b=\{ ans_rule(10) \},
END_TEXT
ANS(num_cmp($d));
BEGIN_TEXT
and f(t) = \{ ans_rule(40) \}
$BR
END_TEXT
ANS(fun_cmp($soln, vars=>"t"));

ENDDOCUMENT();        # This should be the last executable line in the problem.
