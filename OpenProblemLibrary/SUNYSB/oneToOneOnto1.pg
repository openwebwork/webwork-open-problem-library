## DESCRIPTION
## Algebra
## ENDDESCRIPTION

## Tagged by cmd6a 8/6/06

## DBsubject(Algebra)
## DBchapter(Inverse functions)
## DBsection(1-1 functions)
## Institution(SUNYSB)
## Level(2)
## KEYWORDS('algebra','one-to-one')

# generated by LogicPhrases.java on Mon Jan 29 21:10:42 EST 2001
DOCUMENT();
loadMacros(
  "PGstandard.pl",
  "PGchoicemacros.pl",
  "PGcourse.pl"
);
TEXT(beginproblem()); 
$showPartialCorrectAnswers = 0;

#######;

$a = random(1,8);
$b = random(1,8);
$c = random(1,8);

$tf = new_select_list();
$tf -> qa("\(f(x)=\left|x\right|\)", "a",
	  "\(f(x)=x^2+$a\)", "a",
	  "\(f(x)=x^3+$b\)", "d",
	  "\(f(x)=x + $c\left|x\right|\)", "a",
	  "\(f(x)=x(x-$b)(x+$b)\)", "c",
	  "\(f(x)=e^x\)", "b",
	  "\(f(x)=\sin(x)\)", "a"
	  );

$tf -> choose(7);

BEGIN_TEXT
    For each of the following functions, 
    \(f:\textbf{R}\longrightarrow\textbf{R}\) state whether they are one-to-one,
    onto, both or neither. 
    Use the following key:
END_TEXT
TEXT(
    begintable(2),
         row('a', 'neither 1-1 nor onto'),
         row('b', '1-1 but not onto'),
         row('c', 'onto but not 1-1'), 
	 row('d', 'both 1-1 and onto = bijective = invertible'),
    endtable()
);

BEGIN_TEXT
    $PAR
    \{ $tf->print_q() \}
END_TEXT
ANS(str_cmp($tf->ra_correct_ans));

ENDDOCUMENT();








