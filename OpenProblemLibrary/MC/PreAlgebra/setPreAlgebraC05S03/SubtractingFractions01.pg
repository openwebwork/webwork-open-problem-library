##DESCRIPTION
##  Middle School problem:
##ENDDESCRIPTION

## ProblemTester('Laura Travis')
## Publisher('Prentice Hall')

## DBsubject(Arithmetic)
## DBchapter(Fractions/rational numbers)
## DBsection(Addition/subtraction)
## Date(11/12/2012)
## Institution(Mississippi College)
## Author(John Travis)
## MLT(subtractRationalSameDenominators)
## MLTleader(1)
## Level(2)
## MO(1)
## TitleText1('Pre-Algebra')
## KEYWORDS('prealgebra','common core','fractions')

DOCUMENT();

loadMacros('PGstandard.pl', 'PGML.pl', 'contextFraction.pl', 'PGcourse.pl');

Context("Fraction-NoDecimals");
Context()->flags->set(reduceFractions => 0, showProperFractions => 0);

do {
    $atop = random(19, 25);
    $abot = random(5,  7);
    $btop = random(11, 18);
    $bbot = $abot;
} while (gcf($atop - $btop, $abot) == $abot);

$ans = Compute("($atop*$bbot-$btop*$abot)/($abot*$bbot)")->reduce;

Context()->operators->undefine("-", "+");

BEGIN_PGML

Add the following and express your answer as a single fraction.  No decimals or mixed fractions.

>>
[``\frac{[$atop]}{[$abot]} - \frac{[$btop]}{[$bbot]} = ``] [_]{$ans}
<<

END_PGML

$showHint = 3;
BEGIN_PGML_HINT
First you will need to get a common denominator by finding the least common multiple.

END_PGML_HINT

BEGIN_PGML_SOLUTION
Since the two denominators are equal, we can just subtract the numerators:

[```\frac{[$atop]}{[$abot]} - \frac{[$btop]}{[$bbot]} = 
\frac{[$atop] - [$btop]}{[$bbot]} = [$ans]
```]
END_PGML_SOLUTION

ENDDOCUMENT();        