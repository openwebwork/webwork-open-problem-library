##DESCRIPTION
##  Algebra problem: Evaluating the composition of two inverse functions.
##ENDDESCRIPTION

##KEYWORDS('algebra', 'inequality', 'fraction')

## DBsubject('Algebra')
## DBchapter('Composition of two inverse functions')
## DBsection('')
## Date('7/13/2013')
## Author('')
## Institution('')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')

########################################################################

DOCUMENT();      

loadMacros(
   "PGstandard.pl",     # Standard macros for PG language
   "MathObjects.pl",
   "unionTables.pl",
   #"source.pl",        # allows code to be displayed on certain sites.
   #"PGcourse.pl",      # Customization file for the course
);

# Print problem number and point value (weight) for the problem
TEXT(beginproblem());

# Show which answers are correct and which ones are incorrect
$showPartialCorrectAnswers = 1;

##############################################################
#
#  Setup
#
#
Context("Numeric");
Context()->strings->add(undefined=>{});
$a = random(2,10,1);
$b = random($a,15,1);
$c = random($a,15,1);
$d = random(-5,$a,1);


##############################################################
#
#  Text
#
#

Context()->texStrings;
BEGIN_TEXT
$BR
Let \( f(x) = \sqrt{x - $a} \) and \( g(x) = x^{2} +$a \).  Evaluate each of the following.
$BR
$BR
\{
BeginTable(border=>1, tex_border=>"1pt", spacing=>0, padding=>3).
AlignedRow([" \( f(g($b)) = \) "  ,   Compute("$b")->ans_rule(12)   ], align=>LEFT, separation=>0).
AlignedRow([" \( f(g($c)) = \) "  ,   Compute("$c")->ans_rule(12)   ], align=>LEFT, separation=>0).
AlignedRow([" \( g(f($c)) = \) "  ,   Compute("$c")->ans_rule(12)   ], align=>LEFT, separation=>0).
AlignedRow([" \( f(g($d)) = \) "  ,   Compute("$d")->ans_rule(12)   ], align=>LEFT, separation=>0).
AlignedRow([" \( g(f($d)) = \) "  ,   Compute("undefined")->ans_rule(12)   ], align=>LEFT, separation=>0).
EndTable()
\}
$BR
$PAR
If there is no answer, enter "undefined".
$BR
END_TEXT
Context()->normalStrings;

##############################################################
#
#  Answers
#
#


ANS( Compute("$b") -> cmp());
ANS( Compute("$c") -> cmp());
ANS( Compute("$c") -> cmp());
ANS( Compute("$d") -> cmp());
ANS( Compute( "undefined" ) ->cmp());

ENDDOCUMENT();        