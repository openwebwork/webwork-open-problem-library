##DESCRIPTION
## DBsubject(Financial mathematics)
## DBchapter(Equities)
## DBsection(Forwards and futures)
## Institution(NAU)
## Author(Michael Ratliff)
## Level(3)
##ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
  "PGstandard.pl",
  "PGchoicemacros.pl",
  "PGasu.pl",
  "PGcourse.pl"
);

TEXT(&beginproblem);

$amt = random(50,100,5);
$id = random(2,5.5,.5);
$r = random(6,12,1);

$pv = $amt*exp(-$id/100);
$ans1= round(($pv)*100)/100;
&ANS(num_cmp($ans1));

$ans2= round(100*$ans1*exp($r/100))/100;
&ANS(num_cmp($ans2));


TEXT(EV2(<<EOT));

A $DOLLAR $amt stock pays an $id $PERCENT continous dividend. The continously compounded risk-free rate is $r $PERCENT.
$BR
$BR
a) What is the price of a prepaid forward contract that expires 1 year from today, immediately after the fourth-quarter dividend?
$BR
$DOLLAR \{ans_rule(20)\}?
$BR
b) What is the price of a forward contract that expires at the same time? 
$BR
$DOLLAR \{ans_rule(20)\}?
EOT



ENDDOCUMENT();        # This should be the last executable line in the problem.
