# WeBWorK problem written by Alex Jordan
# Portland Community College
#
# Determine if a given number is a solution to a linear equation; fractions involved
#
# Last edited: Jordan 8/23/13
#
# ENDDESCRIPTION

## DBsubject('Algebra')
## DBchapter('Basic Algebra')
## DBsection('Algebraic Expressions')
## KEYWORDS('solution','equation','fraction')
## DBCCSS('6.EE.5')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')
## Author('Alex Jordan, Carl Yao, Chris Hughes')
## Institution('PCC')




##############################################

DOCUMENT();

loadMacros(
  "PGstandard.pl",
  "MathObjects.pl",
  "PGML.pl",
  "parserPopUp.pl",
  "contextFraction.pl",
  "PCCmacros.pl",
  "PGcourse.pl",
);

##############################################

$var = RandomVariableName(type=>variable);

Context("Fraction");
Context()->variables->are($var=>'Real');
#Context()->noreduce('(-x)+y','(-x)-y');
Context()->flags->set(reduceConstants=>0);

#Setting up the equation
do{$an = non_zero_random(-10,10,1);
$ad = non_zero_random(-5,5,1);
$a = Fraction($an,$ad);} until (($a->value)[1] != 1);

$bn = non_zero_random(-10,10,1);
$bd = non_zero_random(-5,5,1);
$b = Fraction($bn,$bd);

do{$cn = non_zero_random(-10,10,1);
$cd = non_zero_random(-5,5,1);
$c = Fraction($cn,$cd);} until ((($c->value)[1] != 1) && ($c != $a));

do{$soln = non_zero_random(-10,10,1);
$sold = non_zero_random(-5,5,1);
$sol = Fraction($soln,$sold);} until (($sol->value)[1] != 1);

$d = $a*$sol+$b-$c*$sol;

$left = Formula("$a $var + $b")->reduce;
$right = Formula("$c $var + $d")->reduce;

#A proposed solution
$x = list_random($sol,$sol,$sol,$sol+1,1/$sol,-$sol);

$isString = "Yes";
$notString = "No";

if ($x==$sol) {$corString = $isString} else {$corString = $notString};

$popup = PopUp(
  ['?',$isString,$notString],
  $corString,
  last => [$notString],
);

##############################################

TEXT(beginproblem());

BEGIN_PGML

Is [`[$x]`] a solution for [`[$var]`] in the equation [`[$left] = [$right]`]? [__]{$popup}

END_PGML

##############################################



if ($x == $sol) {$solnString = 'Since this is a true equation, [`[$x]`] _is_ a solution for [`[$var]`] in the equation [`[$left] = [$right]`].'}
else {$solnString = 'Since this is a false equation, [`[$x]`] _is not_ a solution for [`[$var]`] in the equation [`[$left] = [$right]`].'}

($bn,$bd) = $b->value;
if ($bn > 0) {$pm = '+'} else {$pm = '-'};
$bn = abs($bn);


($dn,$dd) = $d->value;
if ($dn > 0) {$pmd = '+'} else {$pmd = '-'};
$dn = abs($dn);

BEGIN_PGML_SOLUTION
We check to see if [`[$x]`] a solution by evaluating each side of the equation with [`[$var]=[$x]`]. 

    [`\begin{aligned}
    [$left] &\stackrel{?}{=} [$right] \\
    [$a]\left([$x]\right)[$pm] \frac{[$bn]}{[$bd]} &\stackrel{?}{=} [$c]\left([$x]\right)[$pmd] \frac{[$dn]}{[$dd]} \\
    [$a*$x][$pm] \frac{[$bn]}{[$bd]} &\stackrel{?}{=} [$c*$x][$pmd] \frac{[$dn]}{[$dd]}\\
    [$a*$x+$b] &\stackrel{?}{=} [$c*$x+$d]\\
    \end{aligned}
    `]

[$solnString]**

END_PGML_SOLUTION

ENDDOCUMENT();


