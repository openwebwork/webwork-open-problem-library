##DESCRIPTION
## Solve a linear equation

##ENDDESCRIPTION
## DBsubject(Algebra)
## DBchapter(Linear equations and functions)
## DBsection(Linear equations)
## Institution(The College of Idaho)
## Author(RA Cruz)
## MLT(LinearEquations_05)
## Level(3)
## MO(1)
## TitleText1('Essentials of Intermediate Algebra')
## AuthorText1('Blitzer')
## EditionText1('1')
## Section1('1.4')
## Problem1('')
## KEYWORDS('linear equations','solving equations')
## Date: 2007/08

## Updated 2025.02 by Peter Staab, switch to PGML

DOCUMENT();    # This should be the first executable line in the problem.

loadMacros('PGstandard.pl', 'PGML.pl', 'contextFraction.pl', 'PGcourse.pl');

Context('Fraction');

$a = random(2, 3);
do { $b = random(2, 4); } while ($a == $b);
$c = ($b - $a) * random(1, 3);

$x = Fraction($a * $b * $c, $b - $a);

BEGIN_PGML
Solve the equation  for [` x `]:
[```\frac{x}{[$a]} = \frac{x}{[$b]} + [$c]```]

[` x = `] [_]{$x}

END_PGML

$ab  = $a * $b;
$abc = $ab * $c;
$ba  = $b - $a;
BEGIN_PGML_SOLUTION
Multiply the original equation by [$ab]:
[```
\begin{aligned}
[$ab] \frac{x}{[$a]} & = [$ab] \left( \frac{x}{[$b]}+ [$c] \right) \\
[$b] x & = [$a] x + [$abc] \\
[$ba] x & = [$abc] \\
x & = \frac{[$abc]}{[$ba]} = [$x]
\end{aligned}
```]
END_PGML_SOLUTION

#  Answers

Context("LimitedNumeric-StrictFraction");

($num, $den) = reduce($a * $b * $c, $b - $a);
$ans = Compute("$num/$den");
if ($den == 1) { $ans = Compute("$num"); }
# ANS($ans->cmp);

$showPartialCorrectAnswers = 1;

ENDDOCUMENT();
