##DESCRIPTION
##   Graphing Equations

##ENDDESCRIPTION
## DBsubject(Algebra)
## DBchapter(Linear equations and functions)
## DBsection(Graphs of lines)
## Institution(The College of Idaho)
## Author(RA Cruz)
## Level(1)
## MO(1)
## TitleText1('Essentials of Intermediate Algebra')
## AuthorText1('Blitzer')
## EditionText1('1')
## Section1('1.3')
## Problem1('')
## KEYWORDS('graph','points')
## Date: 2007/09

## Updated 2025.02 by Peter Staab, switch to PGML and add a solution.

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros('PGstandard.pl', 'PGML.pl', 'parserPopUp.pl', 'parserRadioButtons.pl', 'PGcourse.pl');

#  Setup

$a = random(2,8);
$c = random(2,3);
if ($c==2) {$b = random(2,6,2);};  #Want the x-intercept to show in the table
if ($c==3) {$b = random(3,9,3);};

@functions = ("x + $a","$a x","$a - x","$b - $c x","$c x + $b");
@x_intercept = (-$a, 0, $a, $b/$c, -$b/$c);
@slice = random_subset(4,0..4);
@fs = map { "\( $_ \)" } @functions[@slice];

$rb = RadioButtons([[@fs]], 0);


$f = Formula($functions[$slice[0]]);     #Choose the first function in the list

@y = map { $f->eval(x => $_)} -3..3;

$dd = DropDown(['Yes', 'No'], $y[3] == 0 ? 0 : 1);

Context("Point");

$xint = Point($x_intercept[$slice[0]],0);
$yint = Point(0,$y[3]);

BEGIN_PGML
Use the table of values given below to solve the following exercises. 

[# 
    [. [`x`] .] [. [`y`] .]*
    [. -3 .] [. [$y[0]] .]*
    [. -2 .] [. [$y[1]] .]*
    [. -1 .] [. [$y[2]] .]*
    [. 0 .] [. [$y[3]] .]*
    [. 1 .] [. [$y[4]] .]*
    [. 2 .] [. [$y[5]] .]*
    [. 3 .] [. [$y[6]] .]*

#]


a)  Which equation corresponds to the points given in the table?

    [_]{$rb}

b) Does the graph of the equation pass through the origin?  [_]{$dd}

c) At which point does the graph of the equation cross the [`x`]-axis? 

    Answer:  [_]{$xint} 

d) At which point does the graph of the equation cross the [`y`]-axis?

    Answer:  [_]{$yint} 
END_PGML

$m = $f->D('x');

BEGIN_PGML_SOLUTION
a) In this case, an increase of one in the [`x`], increases [`y`] by [$m]. Looking at the table, the [`y`]-intercept is [$y[3]].  Thus, the equation is [`[$f]`].

b) Again, looking at the table, the middle of the table shows the [`y`]-intercept is [$y[3]], so the graph does [@ $y[3] == 0 ? '' : 'not' @] pass through the origin. 

c) The point which crosses the [`x`]-axis is [$xint].

d) The point which crosses the [`y`]-axis is [$yint].
END_PGML_SOLUTION


ENDDOCUMENT();