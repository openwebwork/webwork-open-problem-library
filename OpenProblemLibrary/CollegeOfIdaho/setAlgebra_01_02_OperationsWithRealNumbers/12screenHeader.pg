## Static(1)
##Screen set header for Intermediate Algebra: Sec 1.2 Operations With Real Numbers

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
  "PGstandard.pl",
  "PGcourse.pl"
);

BEGIN_TEXT
WeBWorK assignment $setnumber due $formatedDueDate.
$BR $BR
$BBOLD Instructions:$EBOLD
$PAR
$BBOLD Note: $EBOLD Do not rewrite fractions as mixed numbers, i.e. enter \( 3/2 \), but
do not use \( 1 \) \(1/2\).
$BR
END_TEXT

ENDDOCUMENT();        # This should be the last executable line in the problem.

