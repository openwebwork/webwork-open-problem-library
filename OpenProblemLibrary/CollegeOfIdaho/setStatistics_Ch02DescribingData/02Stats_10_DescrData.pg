## DESCRIPTION
## DBsubject(Statistics)
## DBchapter(Exploratory data analysis/descriptive statistics)
## DBsection(Summary statistics)
## Institution(The College of Idaho)
## Author(R Cruz, L Danielson, J Guild)
## MLT(stat-des-concepts)
## MLTleader(1)
## Level(2)
## TitleText1('The Basic Practice of Statistics')
## AuthorText1('David Moore')
## EditionText1('5e')
## Section1('2')
## Problem1('')
## KEYWORDS('statistics', 'skewed', 'five number summary', 'median', 'boxplot')

DOCUMENT(); 

loadMacros('PGstandard.pl', 'PGML.pl', 'parserRadioButtons.pl', 'PGcourse.pl');

@question = (
    'If a distribution is skewed to the right, which of the following is true?',
    'What percent of the observations in a distribution lie between the first quartile and the third quartile?',
    'To make a boxplot of a distribution, you must know which of the following?',
    'Which of the following is least affected if an extreme high outlier is added to your data?'
);

# For each Answer set, the correct answer is in the first slot. 
@answers = (
    ['The mean is greater than the median.',
     'The mean is less than the median.',
	'The mean and median are equal.'],
     ['75%','50%','25%'],
    ['the five-number summary ',
     'all of the individual observations',
	'the mean and the standard deviation'],
    ['the median', 'the mean','the standard deviation']
);

@slice = random_subset(3,0..3);

@rb = map { RadioButtons([$answers[$_],'None of the above.'],0) } @slice; 

$showPartialCorrectAnswers = 0;

BEGIN_PGML
For each problem, select the best response.

a) [$question[$slice[0]]]

    [_]{$rb[0]}

b) [$question[$slice[1]]]

    [_]{$rb[1]}

c) [$question[$slice[2]]]

    [_]{$rb[2]}

END_PGML

@explanations = (
    'The definition of skewed right is that the mean is ' .
    'greater than the median.',
    'Because the first quartile is the middle of the bottom ' .
    'half of the data and the third quartile is the middle ' .
    'of the top half, there is half of the data between these ' .
    'two values',
    'For the box plot, you need the min, max, quartiles and ' .
    'the median.  This is the five-number summary',
    'An outlier can affect the mean and standard deviation. ' .
    'So the least affected (and general not affected at all) ' .
    'is the median'
);

BEGIN_PGML_SOLUTION

a) [$explanations[$slice[0]]]

b) [$explanations[$slice[1]]]

c) [$explanations[$slice[2]]]

END_PGML_SOLUTION

COMMENT('Questions selected randomly from a larger set.  See source for full question list.');

ENDDOCUMENT();       # This should be the last executable line in the problem.