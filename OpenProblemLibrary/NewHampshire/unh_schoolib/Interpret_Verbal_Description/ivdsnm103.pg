## DESCRIPTION
##  Determine the amount of integredient in a punch mixture using ratios. 
## ENDDESCRIPTION

## DBsubject(Arithmetic)
## DBchapter(Fractions/rational numbers)
## DBsection(Ratio/proportions)
## MLT(Application_DirectIntegerRatio)
## MLTleader(1)
## Level(4)

DOCUMENT();        # This should be the first executable line in the problem.
loadMacros('PGstandard.pl', 'PGML.pl', 'parserRadioButtons.pl', 'PGcourse.pl');

do {
    ($a, $b) = (random(2,5), random(2,5));
} until (gcd($a,$b) == 1);

$k= random(2,4);
($g, $j) = ($a*$k, $b*$k); 

# some wrong answers:
($g1, $j1) = ($b, $a);
($g2, $j2) = ($k*$b, $k*$a);
($g3, $j3) = (2*$a, 2*$b+3);

$rb = RadioButtons([["$g quarts of ginger ale, $j quarts of fruit juice ",
        "$g1 cups of ginger ale, $j2 cups of fruit juice",
        "$g2 liters of ginger ale, $j2 liters of fruit juice",
       "$g3 gallons of ginger ale, $j3 gallons of fruit juice"]],0, labels => "ABC");

BEGIN_PGML
A recipe for punch says to mix [$a] parts ginger ale with [$b] parts
fruit juice.  Which mixture satisfies this [$a]:[$b] ratio

[_]{$rb}

END_PGML

BEGIN_PGML_SOLUTION
If we write down the ratios of all of the options, the only one with the [$a]:[$b] (ginger ale to fruit juice) is [$rb].
END_PGML_SOLUTION
ENDDOCUMENT();

