#DESCRIPTION
##Type of
#ENDDESCRIPTION

DOCUMENT();
loadMacros(
  "PGstandard.pl",
  "PGchoicemacros.pl",
  "MathObjects.pl",
  "PGcourse.pl"
);
#for currency use Context("Currency") then Currency($A);
#Then, in the text use $DOLLAR $a
TEXT(beginproblem());
$showPartialCorrectAnswers = 1;
Context("Numeric");
Context()->texStrings;
$radio=new_multiple_choice();
$radio->qa("What is the number? ", "474 ");
$radio->extra("384 ", "24315 ","31524 ");


BEGIN_TEXT

A number has 24 ones, 3 hundreds, and 15 tens.$BR
\{$radio->print_q()\}
\{$radio->print_a()\}

END_TEXT
Context()->normalStrings;
ANS(radio_cmp($radio->correct_ans()));
BEGIN_SOLUTION
$PAR Solution $PAR
The number is \(3\times 100+15\times 10+24\times 1= 474\).$BR  
If you think that the wording of the question was strange and $BR
stilted, I have to agree.
END_SOLUTION

;
ENDDOCUMENT();
## DBsubject(Arithmetic)
## DBchapter(Integers)
## DBsection(Multiple operations)
## Level(2)
## MO(1)

\{ans_rule(10)\}
\{Panic::Button(label => " Ask for a Hint", penalty => .00)\}
END_TEXT
if ($panicked) {
BEGIN_TEXT
   Hint: 
END_TEXT
};
BEGIN_TEXT
$PAR

	ANS(Real(7)->cmp->withPostFilter(AnswerHints(
		"14" => "You must have multiplied by 2 somewhere",
		"-7" => "Check how you got the sign",
		"7" => "Well done!",
		["0"] => "Don't just guess!",
		sub {
			my ($correct,$student,$ans) = @_;
			return $correct . $student == 0;
		}
	
	)));
FOLLOWING WORKS IF LASTANSWER IS E


ANS(radio_cmp($radio->correct_ans())->withPostFilter(sub {
  my $ans = shift;
  my $student = $ans->{student_ans};
  $ans->{ans_message} = "Two angles are supplementary if their degree measures s
um to 180.  In this problem, there are  two angles supplementary to angle CoD. T
wo angles do not have to be adjacent to be supplementary." if $student eq 'E';
  return $ans;
}));

IF YOU ARE PRACTICING FOR THE NECAP EXAMINATION, DO THIS WITHOUT$BR A CALCULATOR. 
 THIS IS A NECAP PROBLEM FOR WHICH A  CALCULATOR $BR WOULD NOT BE ALLOWED.$PAR
