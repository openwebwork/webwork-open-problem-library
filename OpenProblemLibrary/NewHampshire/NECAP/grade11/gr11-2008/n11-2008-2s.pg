#DESCRIPTION
## Calculate the average speed of a boat with different units than was given.
#ENDDESCRIPTION

## DBsubject(Arithmetic)
## DBchapter(Units)
## DBsection(Conversions)
## Level(4)

DOCUMENT();
loadMacros(
    'PGstandard.pl',   'PGML.pl',
    'contextUnits.pl', 'randomPerson.pl',
    'PGcourse.pl'
);

Context("Units")->withUnitsFor('velocity', 'length', 'time');

$p           = randomPerson();
$dist        = random(10, 20, 0.5);
$time        = random(2,  3,  0.1);
$speed_mph   = Compute("($dist mi)/($time h)");
$speed_knots = $speed_mph->toUnits('knots');

BEGIN_PGML
[$p] travels [$dist] miles by boat in [$time] hours.  What is [$p->their] average speed in knots? [__]{$speed_knots->cmp(sameUnits => 1)}

Note: include the units and provide at least 3 digits of precision.  Use 1 knot is 1.151 miles per hour.
END_PGML

BEGIN_PGML_SOLUTION
If [$p->they] [$p->verb('travel')] [$dist] miles in [$time] hours, [$p->they] [$p->verb('travel')] [`\frac{[$dist]}{[$time]}=[@ Round($speed_mph->number,3) @]`] miles per hour

Using dimensional analysis,
[``` [@ Round($speed_mph->number,3) @] \;\text{mph} \cdot \frac{1\; \text{knot}}{1.151\; \text{mph}} =  [$speed_knots] ```]
END_PGML_SOLUTION
ENDDOCUMENT();
