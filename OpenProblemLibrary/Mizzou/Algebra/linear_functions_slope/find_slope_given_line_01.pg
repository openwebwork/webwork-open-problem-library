##DESCRIPTION
##  Lines: Slope
##ENDDESCRIPTION

## BookTitle('Basic Algebra')
## Author1('David Covert')
## Author2('Teri Christiansen')
## Author3('Rick Lynch')
## DBsubject(Algebra)
## DBchapter(Linear equations and functions)
## DBsection(Finding the slope)
## Date(Spring 2012)
## Institution(University of Missouri)
## Level(2)
## TitleText1('College Algebra')
## AuthorText1('Beecher, Penna, Bittinger')
## EditionText1('4')
## Section1('1.2')
## KEYWORDS('lines', 'slope')

DOCUMENT();

loadMacros(
    'PGstandard.pl',      'PGML.pl',
    'contextFraction.pl', 'parserRadioMultiAnswer.pl',
    'PGcourse.pl'
);

Context("Fraction-NoDecimals")->flags(studentsMustReduceFractions => 1);

$a = random(-1, 1, 2) * random(2, 10);
$b = random(-1, 1, 2) * random(2, 5);
$c = random(-1, 1, 2) * random(1, 50);

$m = Fraction(-$a, $b);

# ANS( Compute($m) -> cmp(studentsMustReduceFractions=>1) );

$line = " $a x + $b y = $c";

$rma = RadioMultiAnswer(
    [ ['The slope is undefined.'], [ 'The slope is \(m = \) %s', $m ] ],
    1);

BEGIN_PGML
Find the slope of the line [`[$line]`] if it exists or write *undefined* if the slope is undefined. You must reduce your answer completely.

[_]{$rma}
END_PGML

$rhs1 = Formula("$c-$a x")->reduce;
$rhs2 = $rhs1 / $b;
$rhs3 = Formula("$c/$b - $a/$b *x ")->reduce;

BEGIN_PGML_SOLUTION
Put the equation into slope-intercept form.  Start with the equation
[``` [$line] ```]

[@ $a <0 ? PGML('Then add [`[@-$a@]x`] to both sides.') : PGML('Then subtract [`[$a] x`] from both sides.') @]*

[``` [$b] y = [$rhs1] ```]

Divide through by [`[$b]`] to get

[``` y = [$rhs2] = [$rhs3] ```]

and the slope is the coefficient of the [`x`] term or [` m = [$m] `].

END_PGML_SOLUTION

ENDDOCUMENT();    # This should be the last executable line in the problem.
