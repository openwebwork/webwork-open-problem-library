## DESCRIPTION
## Problem from Functions Modeling Change, Connally, et al., 3rd ed.
## WeBWorK problem written by Adam Spiegler <aspiegler@luc.edu>
## ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Linear Functions and Change')
## DBsection('Geometric Properties of Linear Functions')
## KEYWORDS('lines','parallel','slope-intercept','perpendicular')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('1.5')
## Problem1('24')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "PGauxiliaryFunctions.pl",
           "PGgraphmacros.pl"
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 0;

#$graph = image("sec5_q24.gif", width => 200, height => 200, tex_size => 400);
$m = random(2,5,1);
$k = random(-5,-2,1);
$b = random(1,-$k,1);
$x = -$k/$m;

$f = "2x+1 for x in <-.2,1.5> using color:blue and weight:2";
$g = "2x-2 for x in <-.2,1.5> using color:blue and weight:2";

$graph = init_graph(-.2,-2.5,1.5,4.5,'axes'=>[0,0],'ticks'=>[1,1]);
$graph->lb('reset');
$graph->lb(new Label(0.1,-2,"(0,$k)",'black','left','top'));
$graph->lb(new Label(1.4,1.1,"L",'black','right','top'));
$graph->lb(new Label(1,3,"y=${m}x+$b" ,'black','left','top'));
$graph->lb(new Label(1,-.1,"P",'black','right','top'));
$graph->lb(new Label(1.45,-.1,"x",'black','right','top'));
$graph->lb(new Label(-.1,4.4,"y",'black','right','top'));
$point = closed_circle( 1,0, black );
$graph -> stamps($point);
plot_functions( $graph, $f, $g ); 


BEGIN_TEXT
Question 24:
$BR $SPACE $BR
Line $BITALIC L $EITALIC in the figure below is parallel to the line \( y = $m x + $b \). $SPACE Find the coordinates of the point $BITALIC $BBOLD P $EBOLD $EITALIC $BR
$BCENTER
\{ image(insertGraph($graph), width => 200, height => 200, tex_size => 700) \}
$ECENTER
$BR
P = ( \{ ans_rule(10) \} , \{ ans_rule(10) \} )
END_TEXT

ANS( num_cmp($x) );
ANS( num_cmp(0) );

$neg_k = -$k;

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR
$BBOLD SOLUTION $EBOLD
$BR
Since $BITALIC P $EITALIC is the $BITALIC x${EITALIC}-intercept, it has $BITALIC y${EITALIC}-coordinate 0.  We can calculate the slope of the line L using $BITALIC P $EITALIC \( (x_0, 0) \) and the other given point (0, $k):
$BR
\[ m = \frac{ $k - 0}{0 - x_0} = \frac{$k}{-x_0} = \frac{$neg_k}{x_0} \]
$BR
We know the slope must equal $m since L is parallel to \( y = $m x + b \), and therefore 
$BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( \frac{$neg_$k}{x_0} = $m \)
$BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( x_0 = $neg_$k / $m = $x \)
$BR $SPACE $BR
The point $BITALIC P $EITALIC has coordinates ($x, 0).
$BR
END_SOLUTION


ENDDOCUMENT();   # This should be the last executable line in the problem.