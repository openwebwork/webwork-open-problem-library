# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Exponential Functions')
## DBsection('Review Exercises and Problems')
## KEYWORDS('exponential functions','growth rate','growth factor','continuous exponential growth','e','compound interest','graphs of exponential functions')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('3.Review')
## Problem1('16')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();
loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl",
"PGgraphmacros.pl",
);
 
TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

$a = random(1,3,1);
$b_top = random(3,5,2);
$b_bot = random(2,$top-1,2);
$x0 = random(-3,-2,1);
$x1 = -1*$x0;
$y1_top = $a*$b_top**$x1;
$y1_bot = $b_bot**$x1;
$y1_disp = "$y1_top/$y1_bot";
$y1 = $y1_top/$y1_bot;
$y0_top = $a*$b_bot**$x1;
$y0_bot = $b_top**$x1;
$y0_disp = "$y0_top/$y0_bot";
$y0 = $y0_top/$y1_bot;
$ans = "$a($b_top/$b_bot)^x";

$f = "2*(1.5)^x for x in <-2.5,2.5> using color:blue and weight:2";
$graph = init_graph(-2.5,-.35,2.5,6,'axes'=>[0,0]);
$graph->lb('reset');
$graph->lb(new Label(2.4,-.05,"x",'black','right','top'));
$graph->lb(new Label(-.05,5.75,"y",'black','right','top'));
$graph->lb(new Label(-2,8/9,"($x0, $y0_disp)",'black','left','top'));
$graph->lb(new Label(2,9/2,"($x1, $y1_disp)",'black','right','bottom'));
$point[0] = closed_circle(2,9/2, black );
$point[1] = closed_circle(-2,8/9, black );
$graph -> stamps(@point);
plot_functions($graph, $f );
$fig = image(insertGraph( $graph ), height=>250, width=> 250, 'tex_size'=>600 );

$top_pr = $b_top**(2*$x1);
$bot_pr = $b_bot**(2*$x1);
$b = round(($y1/$y0)**(1/$x1)*10000)/10000;
$ans = "$y0($b_top/$b_bot)^x";
$almost = $b_top**$x1;

BEGIN_TEXT
Question 16:
$BR $SSPACE $BR
Find a possible formula for the exponential function graphed below.
$BR
$BCENTER
$fig
$BR
${BITALIC}(Click on the graph for a larger version.)$EITALIC
$ECENTER
$BR
\( y = \) \{ ans_rule(35) \}

END_TEXT

ANS(fun_cmp( $ans,vars=>'x' ) );

$dx = 2*$x1;

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
Since this function is exponential, we
know \( y = ab^{x} \).  We also know that ($x0, $y0_disp) and ($x1, $y1_disp) are on the graph of this function, so $BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( \displaystyle \frac{$y0_top}{$y0_bot} = ab^{$x0} \ \ \ \ \mbox{and} \ \ \ \ \frac{$y1_top}{$y1_bot} = ab^{$x1} \)
$BR $SPACE $BR
From these two equations, we can say that $BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( \displaystyle \frac{\frac{$y1_top}{$y1_bot}}{\frac{$y0_top}{$y0_bot}} = \frac{ab^${x1}}{ab^{$x0}} \).
$BR
Since \( \ ($y1_top/$y1_bot)/($y0_top/$y0_bot) = \left( $y1_top/$y1_bot \right) \cdot \left( $y0_bot/$y0_top \right) = $top_pr/$bot_pr \ \), we can re-write this equation to be $BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( \displaystyle \frac{$top_pr}{$bot_pr} = b^{$dx} \)
$BR
Keeping in mind that $BITALIC b $EITALIC > 0 (since the graph is increasing), we get $BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( \displaystyle b = \sqrt[$dx]{\frac{$top_pr}{$bot_pr}} = \frac{\sqrt[$dx]{$top_pr}}{\sqrt[$dx]{$bot_pr}} = \frac{$b_top}{$b_bot} \).
$BR
Substituting $BITALIC b $EITALIC = $b_top/$b_bot in \( ab^{$x1} = $y1_disp \), we get $BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( \frac{$y1_top}{$y1_bot} = a \left( \frac{$b_top}{$b_bot} \right)^{$x1} = a \left( \frac{$almost}{$y1_bot} \right) \) $BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( \displaystyle a = \frac{\frac{$y1_top}{$y1_bot}}{\frac{$almost}{$y1_bot}} =
\frac{$y1_top}{$y1_bot} \cdot \frac{$y1_bot}{$almost} = \frac{$y1_top}{$almost} = $a \).
$BR $SPACE $BR
Thus, \( y = $a \left( \frac{$b_top}{$b_bot} \right)^x \).
$BR
END_SOLUTION

ENDDOCUMENT();
