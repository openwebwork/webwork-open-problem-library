# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Logarithmic Functions')
## DBsection('The Logarithmic Functions')
## KEYWORDS('logarithms','log','ln','asymptote','graph of logarithms')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('4.3')
## Problem1('8')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
#           "extraAnswerEvaluators.pl",
#           "PGauxiliaryFunctions.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"PGcourse.pl",
"unionTables.pl",
"parserImplicitPlane.pl",
"parserVectorUtils.pl",
           );
       
TEXT(beginproblem()); # standard preamble to each problem.

$refreshCachedImages=1;

#Context("Numeric");
#Context("Vector2D")->variables->are(x=>"Real",y=>"Real");
Context("ImplicitPlane")->variables->are(x=>"Real",y=>"Real");

@eqn =("\( y= -e^{x} \)", "\( y= e^{-x} \)", "\( y= -e^{-x} \)", "\( y= e^{x} \)");

@reason = ( "The correct graph of \( y= -e^{x} \) is shown above.  It is the graph of \( y = e^x \) flipped over the $BITALIC x${EITALIC}-axis.",
"The correct graph of \( y= e^{-x} \) is shown above.  It is the graph of \( y = e^x \) flipped over the $BITALIC y${EITALIC}-axis.",
"The correct graph of \( y= -e^{-x} \) is shown above.  It is the graph of \( y = e^x \) flipped over the $BITALIC x${EITALIC}-axis and then flipped over the $BITALIC y${EITALIC}-axis.",
"The correct graph of \( y= e^{x} \) is shown above.");

$k = random(0,2,1);
$pick = $eqn[$k];

@perm = NchooseK(4,4);
@mix_graph = @graph[@perm];
@y_int = (-1,1,-1,1);

@inv = invert(@perm);

@letter = ("A", "B", "C", "D");
$correct = $letter[$inv[$k]];
$show_graph = $mix_graph[$inv[$k]];

BEGIN_TEXT

Consider the exponential equation $pick.
$BR
$BR
(a) Determine whether its graph has a horizontal asymptote or a vertical asymptote.  In each answer blank enter an equation \( x = k \) or \( y = k \) for some number ${BITALIC}k,${EITALIC} or, enter ${BITALIC}NONE${EITALIC} if its graph does not have an asymptote.
$BR
The graph has a vertical asymptote with equation 
\{ ans_rule(10) \}
\{ AnswerFormatHelp("equations") \}
$BR
The graph has a horizontal asymptote with equation 
\{ ans_rule(10) \}
\{ AnswerFormatHelp("equations") \}
$BR
$BR
(b)  Determine whether its graph has an x-intercept or a y-intercept.  
In each answer blank enter a point \( (a,b) \) including parentheses, 
or, enter ${BITALIC}NONE${EITALIC} if its graph does not have an intercept.
$BR
The graph has an ${BITALIC}x${EITALIC}-intercept at the point 
\{ ans_rule(10) \}
\{ AnswerFormatHelp("points") \}
$BR
The graph has a ${BITALIC}y${EITALIC}-intercept at the point 
\{ ans_rule(10) \}
\{ AnswerFormatHelp("points") \}

END_TEXT

$showPartialCorrectAnswers = 1;

$VA = Compute("NONE");
$HA = ImplicitPlane("y=0");
$XINT = Compute("NONE");
$YINT = Point("(0,$y_int[$k])");

ANS($VA->cmp( typeMatch=>ImplicitPlane("x+y=1") ) );
ANS($HA->cmp() );
ANS($XINT->cmp( typeMatch=>Point("(1,2)") ) );
ANS($YINT->cmp() );
#ANS(str_cmp($correct) );

#ANS(str_cmp("NONE"));
#ANS(str_cmp("0"));
#ANS(str_cmp("NONE"));
#ANS(str_cmp($y_int[$k]));
#ANS(str_cmp($correct));


SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
$BCENTER
$show_graph
$ECENTER
$BR
$reason[$k]
$BR
END_SOLUTION


COMMENT('MathObject version');
ENDDOCUMENT();
