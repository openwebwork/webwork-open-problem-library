# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Logarithmic Functions')
## DBsection('Logarithms and Their Properties')
## KEYWORDS('logarithms','natural log','common log','log','ln','properties of logs')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('4.1')
## Problem1('45')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
#           "PGchoicemacros.pl",
           "PGanswermacros.pl",
#           "PGgraphmacros.pl",
#           "extraAnswerEvaluators.pl",
#           "PGauxiliaryFunctions.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"PGcourse.pl",
           );
       
TEXT(beginproblem()); # standard preamble to each problem.

Context("Numeric");


$a = random(40,70,2);
$q = random(10,30,2);
$m = random(2,6,1);
$b = random(1,3,1);

$ans1 = Compute("(ln($q/$a) - $b)/$m");

BEGIN_TEXT

Find the exact solution to the equation below. ${BITALIC}(Do not give a decimal approximation.)${EITALIC}
$BR
$BR
\( \displaystyle $a e^{$m x + $b} = $q \)
$BR
$BR
\( x = \) \{ ans_rule(35) \}
\{ AnswerFormatHelp("logarithms") \}

END_TEXT

$showPartialCorrectAnswers = 1;

ANS( $ans1->cmp(tolType=>'absolute',tolerance=>'0.000000000001') );

#ANS( num_cmp( $ans1,tol=>0.000000000001 ) );

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
First we isolate the exponent: $BR
\( \ \ \ \ \ \ \ \ $a e^{$m x + $b} = $q \) $BR
\( \ \ \ \ \ \ \ \ e^{$m x + $b} = \frac{$q}{$a} \) $BR
$SPACE $BR
Once the exponent isolated we use logs and solve for x: $BR
\( \ \ \ \ \ \ \ \ \ln{ \left( e^{$m x + $b} \right) } = \ln{($q/$a)} \) $BR
\( \ \ \ \ \ \ \ \ $m x + $b = \ln{($q/$a)} \) $BR
\( \ \ \ \ \ \ \ \ $m x = \ln{($q/$a)} - $b \) $BR
\( \ \ \ \ \ \ \ \ x = \frac{ \ln{($q/$a)} - $b}{$m} \).
$BR
END_SOLUTION



COMMENT('MathObject version');
ENDDOCUMENT();