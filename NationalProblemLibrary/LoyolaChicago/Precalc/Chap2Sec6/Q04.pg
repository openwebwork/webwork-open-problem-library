# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Functions')
## DBsection('Quadratic Functions')
## KEYWORDS('functions','concavity','quadratic','zeros')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('2.6')
## Problem1('4')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
           "PGgraphmacros.pl"
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 1;

$a = non_zero_random(-16,16,2);
$r[0] = random(-5,5,2);
$r[1] = random(-5,5,2);

if ( $r[0] == $r[1] || $r[0] == -1*$r[1] ){ $r[0]=-1; $r[1]=3 };

$b = $a * ($r[0] + $r[1]);
$c = $a * ($r[0] * $r[1]);
$prod = $r[0]*$r[1];
$sum = $r[0]+$r[1];

for ($i = 0; $i <= 1; $i++){
     if ($r[$i] < 0) { $r[$i] = -1*$r[$i] ; $sign[$i] = "-"} 
                else {$sign[$i] = "+"}; 
     };

BEGIN_TEXT
Question 4:
$BR $SPACE $BR
Decide whether the function $SPACE \( f(t) = $a ( t $sign[0] $r[0] )( t $sign[1] $r[1] ) \) is a quadratic function.
$BR
$SPACE
$BR
If so write the function in standard form, \( f(t) = at^2 + bt + c \).  Below enter the proper values for the constants $BITALIC a${EITALIC}, $BITALIC b${EITALIC}, and $BITALIC c${EITALIC} once the function is put in standard form.  If one of the constants is zero, be sure to enter 0 and do not leave any of answers blank.  Account for subtraction by entering a negative value for the constant.  If the function is not quadratic, enter, $BBOLD NA $EBOLD in each blank.
$BR
$SPACE
$BR
\( f(t) = \) \{ ans_rule(5) \} \( t^2 \) + \{ ans_rule(5) \} \( t \) + \{ ans_rule(5) \}
$BR
END_TEXT

ANS( fun_cmp( $a,vars=>['N','A'] ) );
ANS( fun_cmp( $b,vars=>['N','A'] ) );
ANS( fun_cmp( $c,vars=>['N','A'] ) );


SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
Yes. We rewrite the function giving
$BR
\[ f(t) = $a ( t $sign[0] $r[0] )( t $sign[1] $r[1] )
= $a \left( t^2 + $sum t + $prod \right) = $a t^2 + $b t + $c \] 
So this function is quadratic with $BITALIC a $EITALIC = $a , $BITALIC b $EITALIC = $b , and $BITALIC c $EITALIC = $c .
$BR
END_SOLUTION

ENDDOCUMENT();   # This should be the last executable line in the problem.