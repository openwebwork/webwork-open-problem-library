# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Polynomial And Rational Functions')
## DBsection('The Short-Run Behavior Of Polynomials')
## KEYWORDS('polynomial','zero','factor')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('9.3)
## Problem1('12')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "PGauxiliaryFunctions.pl",
           "extraAnswerEvaluators.pl"
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 1;

$m = random(2,4,1);
$q = random(-4,-3,1);
$r = non_zero_random(-2,-1,1);
$s = random(1,3,1);

$a[4] = $m;
$a[3] = -1-$s*$m-$r*$m-$q*$m;
$a[2] = $s+$r+$r*$s*$m+$q+$q*$s*$m+$q*$r*$m;
$a[1] = -$r*$s-$q*$s-$q*$r-$q*$r*$s*$m;
$a[0] = $q*$r*$s;

$frac = "1/$m";
$final = "$frac, $q, $r, $s";

$f = "$a[4] x^4 + $a[3] x^3 + $a[2] x^2 + $a[1] x + $a[0] for x in <-5,4> using color:blue and weight:2";
$graph = init_graph(-5,-60,4,40,'axes'=>[0,0],'ticks'=>[1,1]);
$graph->lb('reset');
$graph->lb(new Label($q,-2,$q,'black','center','top'));
$graph->lb(new Label($r,-2,$r,'black','center','top'));
$graph->lb(new Label($s,-2,$s,'black','center','top'));
$graph->lb(new Label(1/$m,-2,"1/$m",'black','center','top'));
$graph->lb(new Label($r-.1,2,"y=f(x)",'blue','right','bottom'));
$point[0] = closed_circle( $q,0, black );
$point[1] = closed_circle( $r,0, black );
$point[2] = closed_circle( $s,0, black );
$point[3] = closed_circle( 1/$m,0, black );
$graph -> stamps(@point);
plot_functions( $graph, $f); 
$fig = image(insertGraph($graph), width => 300, height => 300, tex_size => 500);

BEGIN_TEXT
Question 12:
$BR
$SPACE
$BR
Experiment with various viewing windows to determine the zeros of \( f(x)=$a[4] x^4 + $a[3] x^3 + $a[2] x^2 + $a[1] x + $a[0] \).    Then write \( f \) in factored form.
$BR $SPACE $BR
a) The factor which corresponds to the zero that has the smallest value is $SPACE $SPACE \( (x - \) \{ans_rule(5) \} \( ) \).
$BR $SPACE $BR
b) The factor which corresponds to the zero that has the second smallest value is $SPACE $SPACE \( ( x - \) \{ans_rule(5) \} \( ) \).
$BR $SPACE $BR
c) The factor which corresponds to the zero that has the largest value is $SPACE $SPACE \( ( x - \) \{ans_rule(5) \} \( ) \).
$BR $SPACE $BR
d) The remaining factor is of the form $SPACE $SPACE \( ( \) \{ ans_rule(5) \} \( x - 1 ) \).
$BR
END_TEXT

ANS( num_cmp( $q ) );
ANS( num_cmp( $r ) );
ANS( num_cmp( $s ) );
ANS( num_cmp( $m ) );

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
$BCENTER
$fig
$ECENTER
$BR $SPACE $BR
The viewing window \( -5 \leq x \leq 4\) , \(\ -60 \leq y \leq 40 \)
indicates that the zeros of \( f \) occur at approximately \( x= $q \), \( \ x=$r \) , \( \ x=\frac{1}{$m} \), and \( \ x = $s \).  We therefore guess that \( f(x)=(x-$q)(x-$r)($m x-1)(x-$s) \).  We can check this by expanding the product to be sure we obtain \( f(x) \). 
$BR
END_SOLUTION

      
ENDDOCUMENT();