# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Logarithmic Functions')
## DBsection('Review Exercises and Problems')
## KEYWORDS('logarithms','log','ln','half-life','double-time','continuous growth rate')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('4.Review')
## Problem1('6')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
           "PGgraphmacros.pl"
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 1;

$a = random(2,22,2);
$k = random(2.1,3.9,0.2);
$b = round(exp($k)*1000)/1000;

BEGIN_TEXT
Question 6:
$BR $SPACE $BR
Convert the exponential equation to the form \( Q = ab^{t} \).  In blanks, give the corresponding values for the values of $BITALIC a${EITALIC} and $BITALIC b${EITALIC} once you have rewritten the formula.  Round your answer for the base $BITALIC b${EITALIC} to at least three decimal places.
$BR
$SPACE
$BR
\( Q = $a e^{$k t} \) 
$BR
$SPACE $SPACE $SPACE $SPACE $SPACE \( a = \) \{ ans_rule(10) \}
$BR
$SPACE $SPACE $SPACE $SPACE $SPACE \( b = \) \{ ans_rule(10) \} 

END_TEXT

ANS( num_cmp($a) );
ANS( num_cmp($b,tol=>0.0006) );



SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
The continuous percent growth rate is the value of $BITALIC k $EITALIC in the equation \( Q=ae^{kt} \), which is $k.  To convert to the form \( Q=ab^t \), we first say that the right sides of the two equations equal each other (since each equals $BITALIC Q${EITALIC}), and then we solve for $BITALIC a $EITALIC and $BITALIC b $EITALIC.  Thus, we have \( ab^{t}= $a e^{$k t} \).  At $BITALIC t $EITALIC = 0, we can solve for $BITALIC a${EITALIC}: $BR $SPACE $BR

\( \ \ \ \ \ \ ab^{0} = $a e^{$k \cdot 0} \) $BR
\( \ \ \ \ \ \ a \cdot 1 = $a \cdot 1 \) $BR
\( \ \ \ \ \ \ a = $a \). $BR $SPACE $BR

Thus, we have \( $a b^{t} = $a e^{$k t} \), and we solve for \( b \): $BR $SPACE $BR
\( \ \ \ \ \ \ $a b^t = $a e^{$k t} \) $BR
\( \ \ \ \ \ \ b^{t} = e^{$k t} \) $BR
\( \ \ \ \ \ \ b^t = \left( e^{$k} \right)^t \) $BR
\( \ \ \ \ \ \ b = e^{$k} \approx $b \). $BR $SPACE $BR

Therefore, the equation is \( Q = $a \cdot ($b)^{t} \).
$BR
END_SOLUTION
 
ENDDOCUMENT();   # This should be the last executable line in the problem.