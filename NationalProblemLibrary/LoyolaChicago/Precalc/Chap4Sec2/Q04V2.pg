# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Logarithmic Functions')
## DBsection('Logarithms and Exponential Models')
## KEYWORDS('logarithms','exponentials','log','e','ln','continuous growth rate')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('4.2')
## Problem1('4')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

#This problem has been slightly simplified and converted to just asking for b, #a, r, and k. 

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
#           "PGchoicemacros.pl",
           "PGanswermacros.pl",
#           "PGgraphmacros.pl",
#           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
#           "parserPopUp.pl"
"MathObjects.pl",
"AnswerFormatHelp.pl",
"PGcourse.pl",
           );
       
TEXT(beginproblem()); # standard preamble to each problem.

Context("Numeric");
Context()->flags->set(useBaseTenLog=>1);

$showPartialCorrectAnswers = 1;

$a1=random(3,17,2);
$d1 = random(4,10,2);
$e1 = 1/$a1;
$root1 = exp(log($d1)/3);
$k = log($root1);
$r=($root1-1);

BEGIN_TEXT
Give the growth factor \(b\), the starting value \(a\), the growth rate \(r\), and the value of \(k\) when \(Q\) is written in the form \(Q = ae^{kt}\). If there is exponential decay, then your growth rate should negative.
For help entering logarithms, see \{ AnswerFormatHelp("logarithms") \}
$BR
$BR
\( Q = \frac{1}{$a1} \cdot \sqrt[3]{$d1^t} \) has:
$BR
$BR
\( b = \) \{ ans_rule(20) \}
\{ AnswerFormatHelp("numbers") \}
$BR
\( a = \) \{ ans_rule(20) \}
\{ AnswerFormatHelp("numbers") \}
$BR
\( r = \) \{ ans_rule(20) \}
\{ AnswerFormatHelp("numbers") \}
$BR
\( k = \) \{ ans_rule(20) \}
\{ AnswerFormatHelp("numbers") \}
$BR
END_TEXT

#ANS( Real("$root1")->cmp() );
ANS( Compute("e^(ln($d1)/3)")->cmp() );

#ANS( Real("$e1")->cmp() );
ANS( Compute("1/$a1")->cmp() );

#ANS( Real("$r")->cmp() );
ANS( Compute("e^(ln($d1)/3) - 1")->cmp() );

#ANS( Real("$k")->cmp() );
ANS( Compute("ln($d1)/3")->cmp() );

#ANS( num_cmp( $a1 ) );
#ANS( num_cmp($r1,tol=>0.01) );
#ANS( num_cmp($k1,tol=>0.01) );


COMMENT('MathObject version. Uses log = logten');
ENDDOCUMENT();