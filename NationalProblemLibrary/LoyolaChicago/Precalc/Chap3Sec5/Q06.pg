# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Exponential Functions')
## DBsection('Compound Interest')
## KEYWORDS('exponential functions','compound interest','effective rate','nominal rate','continuous exponential growth')
## TitleText1('Functions Modeling Change')
## EditionText1('4')
## AuthorText1('Connally')
## Section1('3.5')
## Problem1('6')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
           "PGgraphmacros.pl"
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 1;

$p = random(5000,7500,1000);
$k = random(11,24,1);
$dec = $k/100;
$b = round(exp($dec)*100000)/100000;
$annual_dec = $b - 1;
$r = $annual_dec*100;

BEGIN_TEXT
Question 6:
$BR $SPACE $BR
Find the effective annual yield and the continuous 
growth rate if \( Q = $p e^{$dec t} \).  Round your answers to the nearest 0.001${PERCENT}. 
$BR $SPACE $BR
a) The continuous growth rate is \{ ans_rule(20) \}$PERCENT $BR
b) The effective annual rate is \{ ans_rule(20) \}$PERCENT 

END_TEXT

ANS(num_cmp($k) );
ANS(num_cmp($r,tol=>0.0006) );

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 

a) The continuous growth rate is $BITALIC k $EITALIC = $dec which is $k${PERCENT}.
$BR $SPaCE $BR
b) To find the effective annual yield, we rewrite the function in the form \( Q=$p \left( e^{$dec} \right)^t = $p b^t \), so $BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\( b = e^{0.19} \approx $b \), $BR
The effective annual yield is 1 - $b = $annual_dec or $r${PERCENT}.
$BR
END_SOLUTION

      
ENDDOCUMENT();   # This should be the last executable line in the problem.