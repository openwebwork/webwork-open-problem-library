# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Polynomial And Rational Functions')
## DBsection('Power Functions')
## KEYWORDS('power function','inversely proportional','proportional')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('9.1)
## Problem1('15')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

DOCUMENT();

loadMacros("PG.pl", 
           "PGbasicmacros.pl",
           "PGanswermacros.pl",
#           "PGstandard.pl",
#           "extraAnswerEvaluators.pl", 
           "PGauxiliaryFunctions.pl", 
#           "PGgraphmacros.pl", 
           "MathObjects.pl", 
#           "PGchoicemacros.pl" 
          # "parserNumberWithUnits.pl",
          # "parserFormulaWithUnits.pl", 
          # "parserFormulaUpToConstant.pl",
           "PGcourse.pl",
"AnswerFormatHelp.pl",
          );

TEXT(beginproblem());

Context("Numeric");

$showPartialCorrectAnswers = 1;


$x1 = random(2,5,1);
$y1 = random(8,12,1);
$y2 = random(16,20,1);

$k = $y1/$x1;
$ans = "$y1/$x1 x";
$top = $x1*$y2;
$x2 = "$top/$y1";


BEGIN_TEXT
Suppose \( y \) is directly proportional to \( x \), and \( y = $y1 \) when \( x= $x1\).
$BR
$BR
(a) Find the constant of proportionality and a formula for \( y \). 
$BR
\( y = \)
\{ ans_rule(20) \} 
\{ AnswerFormatHelp("formulas") \}
$BR
$BR
(b) Use your formula to find \( x \) when \( y= $y2 \).
$BR
\( x = \) \{ ans_rule(20) \} 
\{ AnswerFormatHelp("numbers") \}
END_TEXT

ANS( Compute("$ans")->cmp() );
ANS( Compute("$x2")->cmp() );

#ANS( fun_cmp( $ans, vars=>'x' ) );
#ANS( num_cmp( $x2 ) );


SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
a) $SPACE Substituting into the general formula \( y=kx \), we have \( $y1 =k ($x1) \) or
\( k =\frac{$y1}{$x1} \). So the formula for \( y \) is
$BR $SPACE $BR
\( \ \ \ \ \ \ y= \frac{$y1}{$x1} x \)
$BR $SPACE $BR
b) $SPACE When \( y=$y2 \), we get \( $y2 = \frac{$y1}{$x1} x \ \), so \( \ x = \frac{$x1}{$y1} \cdot $y2 = \frac{$top}{$y1} \).
$BR
END_SOLUTION


COMMENT('MathObject version');
ENDDOCUMENT();