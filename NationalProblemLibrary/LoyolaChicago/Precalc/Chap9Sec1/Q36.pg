# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Polynomial And Rational Functions')
## DBsection('Power Functions')
## KEYWORDS('power function','inversely proportional','proportional')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('9.1)
## Problem1('36')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

DOCUMENT();

loadMacros("PG.pl", 
           "PGbasicmacros.pl",
           "PGanswermacros.pl",
#           "PGstandard.pl",
#           "extraAnswerEvaluators.pl", 
           "PGauxiliaryFunctions.pl", 
#           "PGgraphmacros.pl", 
            "MathObjects.pl", 
#            "PGchoicemacros.pl", 
            "parserPopUp.pl",
"AnswerFormatHelp.pl",
"parserNumberWithUnits.pl",
          );

TEXT(beginproblem());

Context("Numeric")->variables->are(v=>"Real");

#HEADER_TEXT(<<EOF);
#<script language="javascript" type="text/javascript">
#<!-- //
#function unitspopup() {
#    var url = "/webwork2_files/units.html";
#    var opt = "height=625,width=600,location=no,menubar=no,status=no" +
#              "resizable=yes,scrollbars=yes,toolbar=no,";
#    window.open(url,'examdata_info',opt).focus();
#}
#// -->
#</script>
#EOF

$showPartialCorrectAnswers = 1;

$speed = random(50,60,5);

$k = 3.5*$speed;

$ans = "$k/v";
$ans_c = $k/3;
$rd_c = round($ans_c*1000)/1000;

$popup = PopUp(["Select One", 
        "Directly proportional", 
        "Inversely proportional"],
        "Inversely proportional");



BEGIN_TEXT
Driving at $speed mph, it takes approximately 3.5 hours to drive from Long Island to Albany, NY. 
$BR
$BR
(a) Is the time the drive takes directly or inversely proportional to the speed?  Be sure you can explain your reasoning.   \{ $popup->menu() \}
$BR
$BR
(b) Write a formula for the proportion that represents the time the drive takes, \( h \) (in hours), as a function of your speed, \( v \) in mph. 
$BR
\( h(v) = \) \{ ans_rule(20) \}
\{ AnswerFormatHelp("formulas") \}
$BR
$BR
(c) To get to Albany in 3 hours, how fast would you have to drive? 
$BR
\{ ans_rule(15) \} (Include \{ helpLink("units") \} in your answer and round to three decimal places.)
END_TEXT

ANS( $popup->cmp() );
ANS( Compute("$ans")->cmp() );
ANS( NumberWithUnits("$ans_c","mi/hr")->cmp(tolerance=>"0.01",tolType=>"absolute") );

#ANS( $popup->cmp() );
#ANS( fun_cmp( $ans, vars=>'v' ) );
#ANS( num_cmp( $ans_c, units=>'mi/hr', tol=>'0.01' ) );


SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
a) $SPACE The number of hours is inversely proportional to the speed,  because
as the speed, \( v \), increases, the number of hours, \( h \),  decreases.
$BR $SPACE $BR

b) $SPACE Substituting the given values into the general formula \( h= \frac{k}{v} \), we get \( 3.5 = \frac{k}{$speed} \), so \( k = $k \) and the formula is \( h= \frac{$k}{v} \).
$BR $SPACE $BR

c) $SPACE When \( h=3 \), we get \( 3= \frac{$k}{v} \), or \( v= $rd_c \). So getting to Albany in \( 3 \) hours would require the speed of approximately \( $rd_c \) mph.
$BR
END_SOLUTION

ENDDOCUMENT();