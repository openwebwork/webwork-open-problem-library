# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Transformations of Functions and Their Graphs')
## DBsection('Vertical Stretches and Compressions')
## KEYWORDS('reflection','compression','expansion','transformations')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('5.3')
## Problem1('9')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
           "PGgraphmacros.pl"
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 1;

$m = random(1,3,1);
$k = non_zero_random(-5,5,1);
$p = random(1,5,1);
$q = random(2,4,1);

for ($i = -4; $i <= 4; $i++) { $x[$i+4] = $m*$i; $f[$i+4] = $i**2 + $k };
                 
for ($i = 0; $i <= 8; $i++) { $a[$i] = $f[$i] + $p;
                              $b[$i] = -$f[$i];
                              $c[$i] = $q*$f[$i] };

BEGIN_TEXT
Question 9:
$BR
$SPACE
$BR
Values of \( \ f(x) \) are given in the table below:
$BR $SPACE $BR
$BCENTER
\{ begintable(10) \}
\{ row( "$SPACE $SPACE \( x \) $SPACE $SPACE", @x ) \}
\{ row( "$SPACE $SPACE \( f(x) \) $SPACE $SPACE", @f ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
Using the table of values for \( \ f(x) \) , $SPACE complete the table of values for the functions \( \ f(-x) + $p \) , $SPACE \( \ -f(x) \) , $SPACE and \( \ $q f(x) \) $SPACE in their respective tables below:
$BR $HR $BR
$BCENTER
\{ begintable(10) \}
\{ row( "$SPACE $SPACE \( x \) $SPACE $SPACE", @x ) \}
\{ row( "$SPACE $SPACE \( y = f(-x) + $p \) $SPACE $SPACE", ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6) ) \}
\{ endtable() \}
$ECENTER
$BR $HR $BR
$BCENTER
\{ begintable(10) \}
\{ row( "$SPACE $SPACE \( x \) $SPACE $SPACE", @x ) \}
\{ row( "$SPACE $SPACE \( y = -f(x) \)$SPACE $SPACE", ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6)  ) \}
\{ endtable() \}
$ECENTER
$BR $HR $BR
$BCENTER
\{ begintable(10) \}
\{ row( "$SPACE $SPACE \( x \) $SPACE $SPACE", @x ) \}
\{ row( "$SPACE $SPACE \( y = $q f(x) \) $SPACE $SPACE", ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6), ans_rule(6)  ) \}
\{ endtable() \}
$ECENTER
$BR

END_TEXT


ANS( num_cmp( $a[0] ) );
ANS( num_cmp( $a[1] ) );
ANS( num_cmp( $a[2] ) );
ANS( num_cmp( $a[3] ) );
ANS( num_cmp( $a[4] ) );
ANS( num_cmp( $a[5] ) );
ANS( num_cmp( $a[6] ) );
ANS( num_cmp( $a[7] ) );
ANS( num_cmp( $a[8] ) );
ANS( num_cmp( $b[0] ) );
ANS( num_cmp( $b[1] ) );
ANS( num_cmp( $b[2] ) );
ANS( num_cmp( $b[3] ) );
ANS( num_cmp( $b[4] ) );
ANS( num_cmp( $b[5] ) );
ANS( num_cmp( $b[6] ) );
ANS( num_cmp( $b[7] ) );
ANS( num_cmp( $b[8] ) );
ANS( num_cmp( $c[0] ) );
ANS( num_cmp( $c[1] ) );
ANS( num_cmp( $c[2] ) );
ANS( num_cmp( $c[3] ) );
ANS( num_cmp( $c[4] ) );
ANS( num_cmp( $c[5] ) );
ANS( num_cmp( $c[6] ) );
ANS( num_cmp( $c[7] ) );
ANS( num_cmp( $c[8] ) );

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR
$BBOLD SOLUTION $EBOLD
$BR
The three tables are completed below:
$BR $SPACE $BR
$BCENTER
\{ begintable(10) \}
\{ row( "$SPACE $SPACE \( x \) $SPACE $SPACE", @x ) \}
\{ row( "$SPACE $SPACE \( y = f(-x) + $p \) $SPACE $SPACE", @a ) \}
\{ endtable() \}
$ECENTER
$BR $HR $BR
$BCENTER
\{ begintable(10) \}
\{ row( "$SPACE $SPACE \( x \) $SPACE $SPACE", @x ) \}
\{ row( "$SPACE $SPACE \( y = -f(x) \)$SPACE $SPACE", @b  ) \}
\{ endtable() \}
$ECENTER
$BR $HR $BR
$BCENTER
\{ begintable(10) \}
\{ row( "$SPACE $SPACE \( x \) $SPACE $SPACE", @x ) \}
\{ row( "$SPACE $SPACE \( y = $q f(x) \) $SPACE $SPACE", @c  ) \}
\{ endtable() \}
$ECENTER
$BR
END_SOLUTION
      
ENDDOCUMENT();   # This should be the last executable line in the problem.