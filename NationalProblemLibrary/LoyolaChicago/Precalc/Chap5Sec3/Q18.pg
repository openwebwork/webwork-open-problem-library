# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Transformations of Functions and Their Graphs')
## DBsection('Vertical Stretches and Compressions')
## KEYWORDS('reflection','compression','expansion','transformations')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('5.3')
## Problem1('18')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
           "parserPopUp.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"PGcourse.pl",
           );
       
TEXT(beginproblem()); # standard preamble to each problem.

Context("Numeric");

$showPartialCorrectAnswers = 0;

@c = (0.2,0.25,0.5,0.8,0.75,1.5,2.5,3.5,4.5,5.5);
$q = random(0,9,1);
if ($q >=5 ) {$comp = "stretch"; $reason = "stretching"} 
        else {$comp = "compression"; $reason = "compressing"};

@sign = ("+","-");
$i = random(0,1,1);
$j = random(0,1,1);
@lr = ("left","right");
@ud = ("up","down");
$h = random(1,9,2);
$k = random(2,8,2);
$popup1 = PopUp(["?","right","left"],$lr[$i]);
$popup2 = PopUp(["?","stretch","compression"],$comp);
$popup3 = PopUp(["?","up","down"],$ud[$j]);


BEGIN_TEXT
Describe the effect that the transformations \( y = $c[$q] \cdot f(x $sign[$i] $h) $sign[$j] $k \) have on the graph of \( y = f(x) \).  Your numerical answers should all be positive.
\{ AnswerFormatHelp("numbers") \}
$BR
$BR
First take the graph of \( y = f(x) \) and perform a horizontal shift \{ $popup1->menu() \} by \{ ans_rule(3) \} units.
$BR
Next apply a vertical \{ $popup2->menu() \} by a factor of \{ ans_rule(3) \}.
$BR
Finally perform a vertical shift \{ $popup3->menu() \} by \{ ans_rule(3) \} units.
$BR

END_TEXT

ANS($popup1->cmp() );
ANS(Real("$h")->cmp() );
#ANS(num_cmp($h) );

ANS($popup2->cmp() );
ANS(Real("$c[$q]")->cmp() );
#ANS(num_cmp($c[$q]) );

ANS($popup3->cmp() );
ANS(Real("$k")->cmp() );
#ANS(num_cmp($k) );


SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 

The input \( (x $sign[$i] $h ) \) moves the graph $h units to the $lr[$i]. Then the factor of $c[$q] multiplies the resulting $BITALIC y${EITALIC}-values ($reason the graph vertically), and finally the graph is shifted $ud[$j] $k units.
$BR
END_SOLUTION

      

COMMENT('MathObject version');
ENDDOCUMENT();