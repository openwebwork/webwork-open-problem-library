# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Transformations of Functions and Their Graphs')
## DBsection('The Family of Quadratic Functions')
## KEYWORDS('quadratic','vertex','axis of symmetry','zeros')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('5.5')
## Problem1('14')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
#           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
#           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
#           "parserPopUp.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"PGcourse.pl",
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 1;

$p = random(-8,-2,2);
$neg_p = -$p;
$q = random(3,7,2);
$a = random(-4,-2,1);
$x = non_zero_random(-1,2,1);
$x_max = ($p+$q)/2;
$y = $a*($x-$p)*($x-$q);
$y_max = ($a*($x_max-$p)*($x_max-$q))*(1.1) ;
$y_left = $a*(-1)*($p-1-$q);
$y_right = $a*($q+1-$p);
if ($y_left < $y_right) {$y_min = round($y_left)-1}
                   else {$y_min = round($y_right)-1};

if ($x > $x_max) {$dir = "left"} else {$dir = "right"};

$dx = $q-$p+2;
$dy = $y_max-$y_min;
$low = $p-1;
$high = $q+1;

$f = "$a*(x-$q)*(x-$p) for x in <$low,$high> using color:blue and weight:2";
$graph = init_graph($low,$y_min,$high,$y_max,'axes'=>[0,0] );
$graph->lb('reset');
$graph->lb(new Label(-$dx/40,$y_max-$dy/80,"y",'black','right','top'));
$graph->lb(new Label($high-$dx/80,-$dy/40,"x",'black','right','top'));
$graph->lb(new Label($p+$dx/40,0,"($p, 0)",'black','left','top'));
$graph->lb(new Label($q-$dx/40,0,"($q, 0)",'black','right','top'));
$graph->lb(new Label($x,$y+$dy/80,"($x, $y)",'black',$dir,'bottom'));
$point[0] = closed_circle( $q,0, black );
$point[1] = closed_circle( $p,0, black );
$point[2] = closed_circle( $x,$y, black );
$graph -> stamps(@point);
plot_functions( $graph, $f); 
$fig = image(insertGraph($graph),width => 400,height => 400,tex_size => 700);

$ans = "$a*(x+$neg_p)*(x-$q)";

BEGIN_TEXT
Find a formula for the parabola through the points \( ($p,0), ($q,0), \) and \( ($x,$y) \) whose graph is given below.
$BR
$BR
\( f(x) = \) \{ ans_rule(30) \}
\{ AnswerFormatHelp("formulas") \}
$BR
$BR
$BCENTER
$fig
$BR
(Glick on graph to enlarge)
$ECENTER
END_TEXT

ANS( Compute("$ans")->cmp() );

#ANS( fun_cmp( $ans,vars=>'x') );

$almost = ($x - $p)*($x-$q);

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR
The function has zeros at \( \ x= $q \) $SPACE and \( \ x= $p \) , $SPACE and appears quadratic, so it could be of the form \( \ y=a(x+ $neg_p)(x-$q) \) . $SPACE Since \( \ y= $y \) when \( \ x= $x \) $SPACE , we know that \( \ \ y=a($x+$neg_p)($x-$q)= $almost a=$y \) , $SPACE so \( \ a=$a \) . $SPACE  Therefore,  \( \ y =  $a (x+ $neg_p)(x-$q ) \) $SPACE
is a possible formula.
$BR
END_SOLUTION

      

COMMENT('MathObject version');
ENDDOCUMENT();