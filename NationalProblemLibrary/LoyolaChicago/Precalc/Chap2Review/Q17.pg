# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Functions')
## DBsection('Review Exercises and Problems')
## KEYWORDS('functions','domain','range','inverse','function notation')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('2.Review')
## Problem1('17')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
           "PGgraphmacros.pl"
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 1;


$y0 = random(1.9,3.5,.2);
$x0 = random(1.4,4.4,1);

$m = $y0/(-$x0);
$x_max = $x0+1;

$f = "-($y0/$x0) x + $y0 for x in <-1,$x_max> using color:blue and weight:2";
$graph = init_graph(-1,-1,$x0+1,$y0+1,'axes'=>[0,0]);
$graph->lb('reset');
$graph->lb(new Label($x0,-0.1,$x0,'black','right','top'));
$graph->lb(new Label(-0.1,$y0,$y0,'black','right','top'));
$graph->lb(new Label(-0.1,$y0+.8,"y",'black','right','top'));
$graph->lb(new Label($x0+.8,-.1,"x",'black','right','top'));
$graph->lb(new Label($x0+.2,-.5,"y=f(x)",'black','right','top'));
$point[0] = closed_circle( 0,$y0, black );
$point[1] = closed_circle( $x0,0, black );
$graph -> stamps(@point);
plot_functions( $graph, $f); 
$fig = image(insertGraph($graph),width => 200,height => 200,tex_size=>500);

BEGIN_TEXT
Question 17:
$BR $SPACE $BR
Use the graph in the figure below to fill in the missing
values: 
$BR $SPACE $BR
$BCENTER
$fig
$BR
(click on image to enlarge)
$ECENTER
$BR $SPACE $BR
a) \( f(0) = \) \{ ans_rule(10) \} $BR $SPACE $BR
b) \( f( ? ) = 0 \ \)  when ? =  \{ ans_rule(10) \} $BR $SPACE $BR
c) \( f^{-1}(0) = \) \{ ans_rule(10) \} $BR $SPACE $BR
d) \( f^{-1}( ? ) = 0 \ \) when ? = \{ ans_rule(10) \}

END_TEXT

ANS(num_cmp( $y0 ) );
ANS(num_cmp( $x0 ) );
ANS(num_cmp( $x0 ) );
ANS(num_cmp( $y0 ) );


SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
a) Since the vertical intercept of the graph of\( f \) is (0, $y0), we
have \( f(0)=$0 \) .
$BR $SPACE $BR
b) Since the horizontal intercept of the graph of \( f \) is ($x0, 0), we
have \( f($x0)=0 \) .
$BR $SPACE $BR
c) The function \( f^{-1} \) goes from $BITALIC y${EITALIC}-values to $BITALIC x${EITALIC}-values, so to evaluate \( f^{-1}(0) \), we want the $BITALIC x${EITALIC}-value corresponding to $BITALIC y $EITALIC = 0. This is $BITALIC x $EITALIC = $x0, so \( f^{-1}(0)=$x0 \).
$BR $SPACE $BR
d) Solving \( f^{-1}(?)=0 \) means finding the $BITALIC y${EITALIC}-value corresponding to $BITALIC x $EITALIC = 0. This is $BITALIC y $EITALIC = $y0, so \( f^{-1}($y0)=0 \).
$BR
END_SOLUTION

      
ENDDOCUMENT();