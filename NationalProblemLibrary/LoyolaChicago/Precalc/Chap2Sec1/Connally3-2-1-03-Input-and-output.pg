## DESCRIPTION
## Problem from Functions Modeling Change, Connally, et al., 3rd ed.
## Functions: Input and Output
## ENDDESCRIPTION

## KEYWORDS('functions','evaluating','solving','input','output')

## DBsubject('Precalculus')
## DBchapter('Functions')
## DBsection('Input and Output')
## Author('Paul Pearson')
## Institution('Fort Lewis College')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('2.1')
## Problem1('3')


##############################################
#  Initialization

DOCUMENT(); 

loadMacros(
"PGstandard.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"contextFraction.pl",
"PGcourse.pl",
);

TEXT(beginproblem());

##############################################
#  Setup

Context("Fraction-NoDecimals");
Context()->operators->undefine('+','-','*','**','^');

$a = random(2,4,1);
$b = random(3,9,1);
$c = random(3,5,1);
while ($a == $c) { $a = random(2,4,1); }
while ($b == $c) { $b = random(3,9,1); }


##############################################
#  Main text

Context()->texStrings;
BEGIN_TEXT

Suppose \( \displaystyle f(x) = \frac{$b}{$c - x^3} \).
$BR
$BR
(a) Calculate exactly the value of \( y \) when 
\( y = f($a) \).  Simplify your answer as much as possible.  Enter a fraction instead of a decimal.
$BR
y = \{ans_rule(20)\} 
\{ AnswerFormatHelp("fraction") \}


END_TEXT
Context()->normalStrings;

#############################################
#  Answer evaluation

$showPartialCorrectAnswers = 1;

$den = $c - $a**3;
$yanswer = Fraction($b, $den);

ANS($yanswer->cmp(studentsMustReduceFractions=>1,strictFractions=>1,strictMinus=>1,strictMultiplication=>1));



#############################################
#  Main text

Context()->texStrings;
BEGIN_TEXT

$BR
$BR
(b) Calculate exactly the value of \( x \) when \( f(x) = $b \).  Simplify your answer as much as possible.

$BR
x = \{ans_rule(20)\}
\{ AnswerFormatHelp("numbers") \}

END_TEXT
Context()->normalStrings;


##################################################
#  Answer evaluation

#  reset the context
Context("Fraction-NoDecimals");
Context()->operators->redefine('+','-','*','**','^');

$xanswer = Real("($c-1)**(1/3)")->reduce;

ANS($xanswer->cmp());


COMMENT('MathObject version');
ENDDOCUMENT();