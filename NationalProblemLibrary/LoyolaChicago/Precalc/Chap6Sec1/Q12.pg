# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Trigonometric Functions')
## DBsection('Introduction to Periodic Functions')
## KEYWORDS('periodic','period','amplitude','midline')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('6.1')
## Problem1('12')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
#           "PGchoicemacros.pl",
           "PGanswermacros.pl",
#           "PGgraphmacros.pl",
#           "PGauxiliaryFunctions.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"PGcourse.pl",
           );
       
TEXT(beginproblem()); # standard preamble to each problem.

Context("Numeric");

$showPartialCorrectAnswers = 1;

$x[0] = non_zero_random(-9,9,1);
@dx = (2,4,5,8,10,12,15,20);
$j = random(0,7,1);
$inc = $dx[$j];
$max = random(18,8,1);
$min = non_zero_random(-5,4,1);
$mid = ($max+$min)/2;
$other = $max - 2;
$amp = $max-$mid;
if ($other == $mid){ $other = $other-1 };
for ($i = 1; $i <= 8; $i++){ $x[$i] = $x[$i-1] + $inc };
$period = 3*$inc;


BEGIN_TEXT
Let ${BITALIC}y = f(x)${EITALIC} be a periodic function whose values are given below.  Find the period, amplitude, and midline.
$BCENTER
\{ begintable(10) \}
\{ row( "x", @x ) \}
\{ row( "f(x)", $max,$other,$min,$max,$other,$min,$max,$other,$min ) \}
\{ endtable() \}
$ECENTER
$BR
(a) The period of the graph is \{ ans_rule(10) \}
\{ AnswerFormatHelp("numbers") \}
$BR
$BR
(b) The midline of the graph is \{ ans_rule(10) \}
\{ AnswerFormatHelp("numbers") \}
$BR
$BR
(c) The amplitude of the graph is \{ ans_rule(10) \}
\{ AnswerFormatHelp("numbers") \}
END_TEXT

ANS(Compute("$period")->cmp() );
ANS(Compute("$mid")->cmp() );
ANS(Compute("$amp")->cmp() );

#ANS(num_cmp( $period ) );
#ANS(num_cmp( $mid ) );
#ANS(num_cmp( $amp ) );

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
a) The graph appears to have a period of $period.  Every change in the $BITALIC x $EITALIC value of $period brings us back to the same $BITALIC y $EITALIC value.
$BR $SPACE $BR
b) The midline of the graph is $mid since $BR
\( \ \ \ \ \ \ \mbox{amplitude} = \frac{ y_{max} + y_{min} }{2} = \frac{  $max + $min }{2} = $mid \)
$BR $SPACE $BR
c) The amplitude is how much the maximum or minimum $BITALIC y $EITALIC value differs from the average (midline).  Since the maximum $BITALIC y $EITALIC value is $max and the midline is $mid, the amplitude is $max - $mid = $amp.
$BR 
END_SOLUTION

      

COMMENT('MathObject version');
ENDDOCUMENT();