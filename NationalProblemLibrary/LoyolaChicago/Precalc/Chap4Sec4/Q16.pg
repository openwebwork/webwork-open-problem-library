# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Logarithmic Functions')
## DBsection('Logarithmic Scales')
## KEYWORDS('logarithms','log-log scales','fitting exponential date')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('4.4')
## Problem1('16')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "extraAnswerEvaluators.pl",
           "PGauxiliaryFunctions.pl",
           "PGgraphmacros.pl",
           "parserPopUp.pl"
           );
       
TEXT(beginproblem()); # standard preamble to each problem.


$showPartialCorrectAnswers = 1;

$b1 = random(2,3,1);
$b2 = random(4,5,1);
$a = random(2,3,1);
@x = (0,1,2,3,4,5);

for ($j = 0; $j <= 5; $j++) {
              $f[$j] = $b1**$j;
              $logf[$j] = round($j*ln($b1)*10000)/10000;
              $g[$j] = $a*$b2**$j;
              $logg[$j] = round(($j*ln($b2)+ln($a))*10000)/10000};

$diff1 = round(ln($b1)*10000)/10000;
$diff2 = round(ln($b2)*10000)/10000;

$popup = PopUp(["?","Linear","Exponential","Neither Linear nor Exponential"],"Linear");

BEGIN_TEXT
Question 16:
$BR $SPACE $BR
a) Complete the table below with values of \( y = $b1^x \).
$BR $SPACE $BR
$BCENTER
\{ begintable(7) \}
\{ row( "$SPACE $SPACE x $SPACE $SPACE", @x ) \}
\{ row( "\( \ \ y = $b1^x \ \ \)", ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10) ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
b) Complete the table below with values of \( y=\log{ \left( $b1^x \right)} \).
$BR $SPACE $BR
$BCENTER
\{ begintable(7) \}
\{ row( "$SPACE $SPACE x $SPACE $SPACE", @x ) \}
\{ row( "\( \ \ y = \log{($b1^x)} \ \ \)", ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10) ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
c) Based on your values in the table above, what kind of function is \( y=\log{ \left( $b1^x \right)} \)? \{ $popup->menu() \}
$BR $SPACE $BR
d) Complete the table below with values of \( y = $a \cdot $b2^x \).
$BR $SPACE $BR
$BCENTER
\{ begintable(7) \}
\{ row( "$SPACE $SPACE x $SPACE $SPACE", @x ) \}
\{ row( "\( \ \ y = $a \cdot $b2^x \ \ \)", ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10) ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
e) Complete the table below with values of \( y=\log{ \left( $a \cdot $b2^x \right)} \).
$BR $SPACE $BR
$BCENTER
\{ begintable(7) \}
\{ row( "$SPACE $SPACE x $SPACE $SPACE", @x ) \}
\{ row( "\( \ \ y = \log{($a \cdot $b2^x)} \ \ \)", ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10), ans_rule(10) ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
f) Based on your values in the table above, what kind of function is \( y=\log{ \left( $a \cdot $b2^x \right)} \)? \{ $popup->menu() \}


END_TEXT

ANS( num_cmp( $f[0]) );
ANS( num_cmp( $f[1]) );
ANS( num_cmp( $f[2]) );
ANS( num_cmp( $f[3]) );
ANS( num_cmp( $f[4]) );
ANS( num_cmp( $f[5]) );
ANS( num_cmp( $logf[0],tol=>0.0005) );
ANS( num_cmp( $logf[1],tol=>0.0005) );
ANS( num_cmp( $logf[2],tol=>0.0005) );
ANS( num_cmp( $logf[3],tol=>0.0005) );
ANS( num_cmp( $logf[4],tol=>0.0005) );
ANS( num_cmp( $logf[5],tol=>0.0005) );
ANS( $popup->cmp() );
ANS( num_cmp( $g[0]) );
ANS( num_cmp( $g[1]) );
ANS( num_cmp( $g[2]) );
ANS( num_cmp( $g[3]) );
ANS( num_cmp( $g[4]) );
ANS( num_cmp( $g[5]) );
ANS( num_cmp( $logg[0],tol=>0.0005) );
ANS( num_cmp( $logg[1],tol=>0.0005) );
ANS( num_cmp( $logg[2],tol=>0.0005) );
ANS( num_cmp( $logg[3],tol=>0.0005) );
ANS( num_cmp( $logg[4],tol=>0.0005) );
ANS( num_cmp( $logg[5],tol=>0.0005) );
ANS( $popup->cmp() );

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
a) $BR
$BCENTER
\{ begintable(7) \}
\{ row( "$SPACE $SPACE x $SPACE $SPACE", @x ) \}
\{ row( "\( \ \ y = $b1^x \ \ \)", @f ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
b) $BR
$BCENTER
\{ begintable(7) \}
\{ row( "$SPACE $SPACE x $SPACE $SPACE", @x ) \}
\{ row( "\( \ \ y = \log{($b1^x)} \ \ \)", @logf ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
c) The differences between successive terms is constant, approximately $diff1, so the function is linear.
$BR $SPACE $BR
d) $BR
$BCENTER
\{ begintable(7) \}
\{ row( "$SPACE $SPACE x $SPACE $SPACE", @x ) \}
\{ row( "\( \ \ y = $a \cdot $b2^x \ \ \)", @g ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
e) $BR
$BCENTER
\{ begintable(7) \}
\{ row( "$SPACE $SPACE x $SPACE $SPACE", @x ) \}
\{ row( "\( \ \ y = \log{($a \cdot $b2^x)} \ \ \)", @logg ) \}
\{ endtable() \}
$ECENTER
$BR $SPACE $BR
f) The differences between successive terms is constant, approximately $diff2, so the function is linear.
$BR
END_SOLUTION
      
ENDDOCUMENT();
