# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Exponential Functions')
## DBsection('Introduction to the Family of Exponential Functions')
## KEYWORDS('exponential functions','growth rate','growth factor')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('3.1')
## Problem1('14')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

#################################
#  Initialization

DOCUMENT();       

loadMacros(
"PGstandard.pl",
"MathObjects.pl",
"AnswerFormatHelp.pl",
"PGcourse.pl",
);
       
TEXT(beginproblem());


################################
#  Setup
Context("Numeric");

$d = random(2.11,2.61,.01);
$p = random(2,4,1);
$b = round($d**(-$p)*10000)/10000;
$decimal = 1 - $b;
$r = $decimal*100;
$a = random(0.0017,0.0051,0.0001);

$answera = Real("$a")->with(tolType=>'absolute',tolerance=>'.00001');
$answerb = Real("$b")->with(tolType=>'absolute',tolerance=>'.0001');
$answerc = Real("$r")->with(tolType=>'absolute',tolerance=>'.01');


######################################
#  Main text

Context()->texStrings();
BEGIN_TEXT

Find the initial value $BITALIC a${EITALIC}, growth/decay factor $BITALIC b${EITALIC}, and growth/decay rate $BITALIC r${EITALIC} for the following exponential function:
$BR
$SPACE
$BR
\[ Q(t) = $a($d)^{-$p t} \]
$BR
$BR
(a) The initial value is $BITALIC a $EITALIC = 
\{ ans_rule(10) \}
\{ AnswerFormatHelp("numbers") \}
$BR
$BR
(b) The growth/decay factor is $BITALIC b $EITALIC = 
\{ ans_rule(10) \} 
\{ AnswerFormatHelp("numbers") \}
(Round to at least four decimal places.)
$BR
$BR
(c) The growth/decay rate is $BITALIC r $EITALIC = 
\{ ans_rule(10) \} $PERCENT 
\{ AnswerFormatHelp("numbers") \}
(Round to at least the nearest 0.01${PERCENT})

END_TEXT
Context()->normalStrings;


###################################
#  Answer evaluation

$showPartialCorrectAnswers = 1;

ANS($answera->cmp() );
ANS($answerb->cmp() );
ANS($answerc->cmp() );

#ANS(num_cmp($a) );
#ANS(num_cmp($b,tol=>"0.0002") );
#ANS(num_cmp($r) );


###################################
#  Solution

BEGIN_SOLUTION
$BR
$BR 
$BBOLD  SOLUTION $EBOLD
$BR 
We first can rewrite this in standard form, \( Q=ab^t \),
$BR
$SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE $SPACE 
\[ Q= $a ( $d )^{-$p t} = $a \left( $d^{-$p} \right)^t \approx $a ( $b )^t\]
$BR
so \( a = $a \), $SPACE \( b \approx $b \), $SPACE and \( r = 1 - b \approx $decimal \) or approximately $r${PERCENT}.
$BR
END_SOLUTION



COMMENT('MathObject version');
ENDDOCUMENT();