# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Transformations of Functions and Their Graphs')
## DBsection('Reflections and Symmetry')
## KEYWORDS('reflection','odd function','even function','symmetry')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('5.2')
## Problem1('2')
## Author('Adam Spiegler and Paul Pearson')
## Institution('Loyola University Chicago and Fort Lewis College')

DOCUMENT();       

loadMacros("PG.pl",
           "PGbasicmacros.pl",
#           "PGchoicemacros.pl",
           "PGanswermacros.pl",
#           "PGgraphmacros.pl",
#           "extraAnswerEvaluators.pl",
#           "PGauxiliaryFunctions.pl",
"MathObjects.pl",
"parserVectorUtils.pl",
"AnswerFormatHelp.pl",
"PGcourse.pl",
           );
       
TEXT(beginproblem()); # standard preamble to each problem.

Context("Vector2D");

$showPartialCorrectAnswers = 1;

$x = random(-9,9,2);
$y = non_zero_random(-8,8,2);

$ans_a1 = - $x;
$ans_a2 = $y;

$ans_b1 = $x;
$ans_b2 = - $y;

$ans_c1 = - $x;
$ans_c2 = $y;

$ans_d1 = - $x;
$ans_d2 = - $y;

BEGIN_TEXT
The graph of ${BITALIC}y = g(t)${EITALIC} contains the point \( ($x, $y) \).  What point must be on the following graphs?
Enter points as \( (a,b) \) including parentheses.
$BR
$BR
(a) The graph of \( g(-t) \) must contain the point \{ ans_rule(10) \}
\{ AnswerFormatHelp("points") \}
$BR
$BR
(b) The graph of \( -g(t) \) must contain the point  \{ ans_rule(10) \}
\{ AnswerFormatHelp("points") \}
$BR
$BR
(c) If \( g(t) \) is an even function, then graph of \( g(t) \) must also contain the point \{ ans_rule(10) \}
\{ AnswerFormatHelp("points") \}
$BR
$BR
(d) If \( g(t) \) is an odd function, then graph of \( g(t) \) must also contain the point \{ ans_rule(10) \}
\{ AnswerFormatHelp("points") \}

END_TEXT

ANS(Point("($ans_a1,$ans_a2)")->cmp() );
ANS(Point("($ans_b1,$ans_b2)")->cmp() );
ANS(Point("($ans_c1,$ans_c2)")->cmp() );
ANS(Point("($ans_d1,$ans_d2)")->cmp() );


#ANS(num_cmp($ans_a1) );
#ANS(num_cmp($ans_a2) );
#ANS(num_cmp($ans_b1) );
#ANS(num_cmp($ans_b2) );
#ANS(num_cmp($ans_c1) );
#ANS(num_cmp($ans_c2) );
#ANS(num_cmp($ans_d1) );
#ANS(num_cmp($ans_d2) );

if ($x < 0) {$word_c = "right"} else {$word_c = "left"};
if ($y < 0) {$word_d = "above"} else {$word_d = "below"};
$neg_x = -$x;
$neg_y = -$y;

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
a) The negative sign inside causes a horizontal reflection across the $BITALIC P${EITALIC}-axis giving ($neg_x,$y).
$BR $SPACE $BR
b) The negative sign outside causes a vertical reflection across the $BITALIC t${EITALIC}-axis giving ($x,$neg_y).
$BR $SPACE $BR
c) Even symmetry means that the function is symmetric about the $BITALIC P${EITALIC}-axis, so the $BITALIC P${EITALIC}-coordinate is unchanged, but the  $BITALIC t${EITALIC}-coordinate is the same distance to the $word_c of the  $BITALIC P${EITALIC}-axis, so the point is ($neg_x,$y). 
$BR $SPACE $BR
d) Odd symmetry means that the function is symmetric about the origin.  When we evaluate the function at the same distance to the $word_c of the $BITALIC P${EITALIC}-axis, the $BITALIC P $EITALIC value is reflected to the same distance $word_d the $BITALIC t${EITALIC}-axis, so the point is ($neg_x,$neg_y). 
$BR

END_SOLUTION


COMMENT('MathObject version');
ENDDOCUMENT();