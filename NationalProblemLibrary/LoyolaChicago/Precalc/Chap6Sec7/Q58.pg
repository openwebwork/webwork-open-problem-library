# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Trigonometric Functions')
## DBsection('Inverse Trigonometric Functions')
## KEYWORDS('arctan','arcsin','arccos','reference angle')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('6.7')
## Problem1('58')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();

loadMacros("PG.pl", 
           "PGbasicmacros.pl",
           "PGanswermacros.pl",
           "PGstandard.pl",
           "extraAnswerEvaluators.pl", 
           "PGauxiliaryFunctions.pl", 
           "PGgraphmacros.pl", 
           "MathObjects.pl", 
           "PGchoicemacros.pl" 
          # "parserNumberWithUnits.pl",
          # "parserFormulaWithUnits.pl", 
          # "parserFormulaUpToConstant.pl",
          # "PGcourse.pl",
          );

TEXT(beginproblem());


$showPartialCorrectAnswers = 1;

BEGIN_TEXT
Question 58:
$BR
$SPACE
$BR
State the domain and range of each of the following functions:
$BR
$HR
As always type $BBOLD pi $EBOLD for \( \pi \).  If you wish to enter \( - \infty \), type $BBOLD -infinity $EBOLD (no space between - sign and word).  If you wish to enter \( \infty \), type $BBOLD infinity $EBOLD .
$HR
$BR
$SPACE
$BR
a) \( f(x) = \arccos{(x)} \) has 
$BR
$SPACE $SPACE $SPACE $SPACE Domain: \{ ans_rule(20) \} \( \leq x \leq \) \{ ans_rule(20) \}
$BR
$SPACE $SPACE $SPACE $SPACE Range: \{ ans_rule(20) \} \( \leq f(x) \leq \) \{ ans_rule(20) \}
$BR
$SPACE
$BR
b) \( g(x) = \arcsin{(x)} \) has 
$BR
$SPACE $SPACE $SPACE $SPACE Domain: \{ ans_rule(20) \} \( \leq x \leq \) \{ ans_rule(20) \}
$BR
$SPACE $SPACE $SPACE $SPACE Range: \{ ans_rule(20) \} \( \leq g(x) \leq \) \{ ans_rule(20) \}
$BR
$SPACE
$BR
c) \( h(x) = \arctan{(x)} \) has 
$BR
$SPACE $SPACE $SPACE $SPACE Domain: \{ ans_rule(20) \} \( < x < \) \{ ans_rule(20) \}
$BR
$SPACE $SPACE $SPACE $SPACE Range: \{ ans_rule(20) \} \( < h(x) < \) \{ ans_rule(20) \}
$BR
$SPACE
$BR


END_TEXT


ANS( num_cmp( -1 ) );
ANS( num_cmp( 1 ) );
ANS( num_cmp( 0 ) );
ANS( num_cmp( pi ) );
ANS( num_cmp( -1) );
ANS( num_cmp( 1 ) );
ANS( num_cmp( - pi/2 ) );
ANS( num_cmp( pi/2 ) );
ANS( std_str_cmp( "-infinity" ) );
ANS( std_str_cmp( "infinity" ) );
ANS( num_cmp( - pi/2 ) );
ANS( num_cmp( pi/2 ) );


SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR
a) The domain of \( f\) is \( -1 \le x \le 1 \), so \( \arccos{(x)}\) is
defined only if \(x\) is between \(-1\) and \(1\) inclusive.
The range is between \(0\) and \(\pi\) inclusive.
$BR $SPACE $BR
b) The domain of \(g\) is \(-1 \le x \le 1\), so \(\arcsin{(x)}\) is
defined only if \(x\) is between \(-1\) and \(1\) inclusive.
The range is between \(-\frac{\pi}{2}\) and \( \frac{\pi}{2} \) inclusive.
$BR $SPACE $BR
c) The domain of \(h\) is all real numbers, so \(\arctan{(x)}\) is
defined for all values of \(x\).
The range is between but not including \(-\frac{\pi}{2} \) and \( \frac{\pi}{2}\).
$BR
END_SOLUTION


COMMENT('MathObject version');
ENDDOCUMENT();
