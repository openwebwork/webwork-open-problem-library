# DESCRIPTION
# Problem from Functions Modeling Change, Connally et al., 3rd ed.
# WeBWorK problem written by Adam Spiegler, <aspiegler@luc.edu>
# ENDDESCRIPTION

## DBsubject('Precalculus')
## DBchapter('Trigonometric Functions')
## DBsection('Radians')
## KEYWORDS('sine','cosine','radians','degrees')
## TitleText1('Functions Modeling Change')
## EditionText1('3')
## AuthorText1('Connally')
## Section1('6.3')
## Problem1('28')
## Author('Adam Spiegler')
## Institution('Loyola University Chicago')

DOCUMENT();

loadMacros("PG.pl", 
           "PGbasicmacros.pl",
           "PGanswermacros.pl",
           "PGstandard.pl",
           "extraAnswerEvaluators.pl", 
           "PGauxiliaryFunctions.pl", 
           "PGgraphmacros.pl", 
           "MathObjects.pl", 
           "PGchoicemacros.pl" 
          # "parserNumberWithUnits.pl",
          # "parserFormulaWithUnits.pl", 
          # "parserFormulaUpToConstant.pl",
          # "PGcourse.pl",
          );

TEXT(beginproblem());


$showPartialCorrectAnswers = 1;


# we graph the function x^2 + y^2 = 5^2 and use labels to fudge r.
$s = random(2,7,1);
$r = random($s+2,$s+6,1);

$graph[0] = init_graph(0,0,6,6,'axes'=>[0,0],'grid'=>[1,1]);
$graph[0]->lb('reset');
for($j = $r; $j <= $r; $j++){
     $graph[0]->lb(new Label(5,.02,$j,'black','left','bottom'));
     $graph[0]->lb(new Label(.02,5,$j,'black','left','bottom'))
     };
$graph[0]->lb(new Label(.75,.2,"Q",'black','left','bottom'));
$graph[0]->lb(new Label(2,1.8,"r",'black','left','bottom'));
$graph[0]->lb(new Label(4.8,1.8,"s = $s",'black','left','bottom'));
$graph[0]->lb(new Label(4,3.1,"P",'black','left','bottom'));

$f1[0] = "(25-x^2)^(1/2) for x in <0,4> using color:red and weight:1";
$f2[0] = "(25-x^2)^(1/2) for x in <4,5> using color:blue and weight:2";
$f3[0] = "(3x)/4 for x in <0,4> using color:blue and #weight:2";
$f4[0] = "(1-x^2)^(1/2) for x in <.8,1> using color:blue and weight:2";

$point = closed_circle( 4, 3, black );
$graph[0] -> stamps($point);

plot_functions( $graph[0], $f1[0], $f2[0], $f3[0], $f4[0]);
##############################################################

$radian = "$s/$r";
$degree = round( $s/$r * 180/ pi);
$x = round( $r*cos($s/$r) *1000)/1000 ;
$y = round( $r*sin($s/$r) *1000)/1000 ;

BEGIN_TEXT
Question 28:
$BR
$SPACE
$BR
Using the graph below, find values for the radius $BBOLD r${EBOLD}, the angle \( \mathbf{\theta} \) (in both degrees and radians), and the coordinates for the point $BBOLD P $EBOLD labeled.  $BITALIC (Note: The angle \( \theta \) is labeled $BBOLD Q $EBOLD on the graph.) $EITALIC
$BCENTER
\{  image(insertGraph($graph[0]),'tex_size'=>500) \}
$BR
${BITALIC}(Click on the graph to get a larger version.)$EITALIC
$ECENTER
$BR
a) \( r = \) \{ ans_rule(10) \}
$BR
$SPACE 
$BR
b) The angle \( \theta \) is (give an exact answer) \{ ans_rule(10) \} $BBOLD RADIANS $EBOLD
$BR
$SPACE 
$BR
c) The angle \( \theta \) is (round to nearest whole degree) \{ ans_rule(10) \} $BBOLD DEGREES $EBOLD
$BR
$SPACE 
$BR
d) The coordinates for the point $BBOLD P $EBOLD are ( \{ ans_rule(10) \} , \{ ans_rule(10) \} ).  (round the values for the x and y coordinates to three decimal places)
$BR
END_TEXT

ANS(num_cmp( $r ) );
ANS(num_cmp( $radian ) );
ANS(num_cmp( $degree ) );
ANS(num_cmp( $x,tol=>0.01 ) );
ANS(num_cmp( $y,tol=>0.01 ) );

SOLUTION(EV3(<<'END_SOLUTION'));
$BR $SPACE $BR 
$BBOLD  SOLUTION $EBOLD
$BR 
a) The radius is \( r = $r \) since the \(x\)-intercept is labeled at \( (7,0) \).
$BR $SPACE $BR
b) Using the formula \( s= r \theta \), we have \( \theta= \frac{s}{r} = \frac{$s}{$r} \) radians
$BR $SPACE $BR 
c) Converting the answer in (b), $SPACE \( \frac{$s}{$r} \cdot \frac{180^{\circ}}{\pi} = $degree^{\circ} \)
$BR $SPACE $BR
d) The coordinates of  \( P \)  are  \( \left( r \cos{\theta} , \ r \sin{\theta} \right) \approx ($x, $y) \)
$BR
END_SOLUTION



COMMENT('MathObject version');
ENDDOCUMENT();