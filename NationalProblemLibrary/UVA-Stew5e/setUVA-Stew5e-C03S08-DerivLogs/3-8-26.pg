## DESCRIPTION
##  Calculus: Derivatives of Logarithms
## ENDDESCRIPTION

##KEYWORDS('derivatives', 'logarithmic functions')
## Tagged by YL

## DBsubject('Calculus')
## DBchapter('Differentiation')
## DBsection('Derivatives of Logarithmic Functions')
## Date('5/27/2005')
## Author('Jeff Holt')
## Institution('UVA')
## TitleText1('Calculus')
## EditionText1('5e')
## AuthorText1('Stewart')
## Section1('3.8')
## Problem1('26')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGgraphmacros.pl",
           "PGnumericalmacros.pl",
           "extraAnswerEvaluators.pl"
           );

TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

$a = random(5,18,2);
$b = random(2,6,2);
$c = random(3,7,2);

BEGIN_TEXT
Suppose that
\[ f(x) = \frac{$a}{$b + \ln($c x)}.\]
Find  \(f'(x)\), and use interval notation to give the domain of \(f\).
$BR
$BBOLD Note: $EBOLD When entering interval notation in WeBWorK, use
$BBOLD I $EBOLD for \(\infty\), $BBOLD -I $EBOLD for \(-\infty\),
and $BBOLD U $EBOLD for the union symbol.  If the set is empty,
enter $BBOLD (0,0) $EBOLD.
$PAR
\(f'(x)\) = \{ ans_rule(35) \}
$PAR
Domain = \{ ans_rule(35) \}
$PAR

END_TEXT

ANS((fun_cmp("(- $a/($b+ln($c*x))**2)*(1/x)", vars=>"x"),
    interval_cmp("(-I,exp(-$b)/$c)U(exp(-$b)/$c,I)") ) );

ENDDOCUMENT();        # This should be the last executable line in the problem.
