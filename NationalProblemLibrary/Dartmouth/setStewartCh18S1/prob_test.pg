## -*- perl -*- ##
## Line above puts emacs in perl mode
##
## Description  
##   Second Order Constant Coefficient ODE (distinct real roots)
## EndDescription

DOCUMENT();
loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGauxiliaryFunctions.pl",
           );
 

## Do NOT show partial correct answers
$showPartialCorrectAnswers = 0;


$root1 = random(1,10);

## Ok, we are ready to begin the problem...
##
TEXT(beginproblem());


BEGIN_TEXT
$BR
This is a bare bones test problem to try to isolate an error.  Guess a number from 1 to 10:

\(n = \) \{NAMED_ANS_RULE(first_num,25)\} 
$BR

$PAR
END_TEXT

($ans_eval1) = num_cmp($root1);             ## Value of $function1 at t = 1
$firstAnswer = $inputs_ref->{first_num};         ## Grab the first answer entered by student
$firstAnswer = '' unless defined($firstAnswer);  ## Make sure it is defined

$rh_ans_hash = &$ans_eval1($firstAnswer);        ## Check first answer against given


## Check answers accordingly
if ( $rh_ans_hash->{score} == 1 ) 
{
    NAMED_ANS( first_num, num_cmp($root1) );
    }
else
{   
    NAMED_ANS( first_num, num_cmp($root1) );

}

ENDDOCUMENT();




