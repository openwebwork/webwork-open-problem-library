## -*- perl -*- ##
## Line above puts emacs in perl mode
##
## Description  
##   Directional Derivatives
## EndDescription

DOCUMENT();
loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGauxiliaryFunctions.pl",
           "PGgraphmacros.pl",
           "Dartmouthmacros.pl");
 

## Do NOT show partial correct answers
$showPartialCorrectAnswers = 0;

$a = random(-5,5);
$b = non_zero_random(-5,5);

$c = non_zero_random(-5,5);
$d = non_zero_random(-5,5);


## Ok, we are ready to begin the problem...
##
TEXT(beginproblem());

BEGIN_TEXT
$BR

Find the directional derivative of \(f(x,y) = x/y\) at the point
($a, $b) in the direction of the vector
\(\mathbf v = \langle $c, $d \rangle \).
$BR
\{ans_rule(60)\}

If you didn't get the right answer, it is probably not a syntax error.
There is something different in this problem than the previous ones.

$PAR
END_TEXT

@gradf = (1/$b, -$a/(($b)**2));
@v = ($c, $d);
$length = vector_length(@v);

@u = scalar_mult_vector(1/$length, @v); 

ANS(num_cmp(dot_product(@gradf, @u)));

ENDDOCUMENT();




