##DESCRIPTION
##  Relating equation of tangent line to given limit
##KEYWORDS('limits', 'tangent line')
##Authored by Zig Fiedorowicz, July, 2001
##ENDDESCRIPTION


DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

$showPartialCorrectAnswers = 1;

$a = random(2,7,1);
$b = -random(2,11,1);
$c = random(-1,1,2)*random(3,12,1),
$ans1 = "$c*(x - $a) + $b";

TEXT(&beginproblem);
BEGIN_TEXT
Assume that \(f(x)\) is everywhere continuous and it is given to you that
\[\lim_{x\to $a}\frac{f(x) - $b}{x - $a} = $c \]
It follows that \(y =\) \{ans_rule()\} is the equation of the tangent line
to \(y = f(x)\) at the point \((\)\{ans_rule(5)\}\(,\)\{ans_rule(5)\}\()\).
END_TEXT

&ANS(function_cmp($ans1));
&ANS(std_num_cmp($a));
&ANS(std_num_cmp($b));

ENDDOCUMENT();        # This should be the last executable line in the problem.

