#DESCRIPTION
#Sequences. Find the limit.
#ENDDESCRIPTION

#KEYWORDS('Sequences', 'convergence')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(&beginproblem);
$showPartialCorrectAnswers = 1;

$a = random(2,9,1);
$b = random(3,7,1);
$c = random(2,5,1);
$d = random(3,8,1);
$m = random(2,5,1);
$n = random(3,6,1);

$ans1 = "$m*x^($m-1)*sin(x^$n)+$n*x^($m+$n-1)*cos(x^$n)";
$ans2 = "$a*x/sqrt($a*x^2-$b)";
$ans3 = "($c*($a*x^2+$d)-2*$a*x*($c*x+$b))/($a*x^2+$d)^2";

BEGIN_TEXT
Find the derivative of
\[f(x) = x^{$m}\sin(x^{$n})\]
\(f'(x) = \) $BR
\{ans_rule(60)\}
$PAR
Find the derivative of
\[g(x) = \sqrt{$a x^2 - $b}\]
\(g'(x) = \) $BR
\{ans_rule(60)\}$BR
Find the derivative of
\[h(x)=\frac{$c x + $b}{$a x^2 + $d}\]
\(h'(x) = \) $BR
\{ans_rule(60)\}$BR
END_TEXT

&ANS(function_cmp($ans1));
&ANS(function_cmp($ans2,"x",$b+1,$b+2));
&ANS(function_cmp($ans3));

ENDDOCUMENT();        # This should be the last executable line in the problem.

