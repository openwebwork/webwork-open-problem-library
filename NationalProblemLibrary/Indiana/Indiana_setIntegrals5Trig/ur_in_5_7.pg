##DESCRIPTION
##KEYWORDS('integrals', 'trigonometric')
##ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(&beginproblem);
$showPartialCorrectAnswers = 1;

$b1 = random(2,15,1);
$b2 = random(2,15,1);
$a = random(2,20,1);
$pi = arccos(-1);
$soln = (tan($a*$pi/$b1)-tan(-$a*$pi/$b2))/$a-($pi/$b1+$pi/$b2);

TEXT(EV2(<<EOT));

Evaluate the definite integral.
$BR \[ \int_{\frac{-\pi}{$b2}}^{\frac{\pi}{$b1}} \tan^2($a x) dx \]
$BR $BR \{ans_rule( 30) \}
$BR
EOT

$ans = $soln;
&ANS(std_num_cmp($ans));

&SOLUTION(EV3(<<'EOT'));

$SOL $BR $BR

We use the strategy taught in the first red box on the top of page 480 of
Stewart's Calculus text:

\[
\begin{align*}
\int_{\frac{-\pi}{$b2}}^{\frac{\pi}{$b2}} \tan^2($a x)dx
&=	\int_{\frac{-\pi}{$b2}}^{\frac{\pi}{$b2}}
		\left( \sec^2($a x) - 1 \right) dx	\\\\
&=	\int_{\frac{-\pi}{$b2}}^{\frac{\pi}{$b2}} \sec^2($a x) dx
		- \int_{\frac{-\pi}{$b2}}^{\frac{\pi}{$b2}} dx	\\\\
&=	\left( \left \frac{\tan($a x)}{$a}
			\right|_{\frac{-\pi}{$b2}}^{\frac{\pi}{$b2}} \right)
		- \left( \left x \right|_{\frac{-\pi}{$b2}}^{\frac{\pi}{$b2}}
			\right)	\\\\
&=	\left( \frac{\tan\left(\frac{$a \pi}{$b1}\right)
			-\tan\left(\frac{-$a \pi}{$b2}\right)}{$a} \right)
		- \left( \frac{\pi}{$b1}-\left(-\frac{\pi}{$b2}\right) \right)
				\\\\
&=	$ans
\end{align*}
\]

EOT

ENDDOCUMENT();        # This should be the last executable line in the problem.

