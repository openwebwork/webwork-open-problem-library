##DESCRIPTION
##  Plug a value into a formula from a word problem 
##
##ENDDESCRIPTION
## DBsubject('Intermediate Algebra')
## DBchapter('Ch 01: Algebra, Mathematical Models and Problem Solving')
## DBsection('Algebraic Expressions and Real Numbers')
## KEYWORDS('real numbers','algebraic expressions')
## TitleText1('Essentials of Intermediate Algebra')
## EditionText1('1')
## AuthorText1('Blitzer')
## Section1('1.1')
## Problem1('')
## Author('RA Cruz')
## Institution('The College of Idaho')
## Date: 2007/08

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
  "PGstandard.pl",
  "parserNumberWithUnits.pl",
  "MathObjects.pl"
);

TEXT(beginproblem());

######################################
#  Setup

Context()->variables->add(F=>'Real');

$F = random(0,105,1);
$C = Formula("(5/9)(F - 32)");

#For later when we have a fraction class
#put this in the display text: \[ C = \{ $C->TeX \} \]

######################################
#  Main text

BEGIN_TEXT
The formula
\[ C = \frac{5}{9}(F - 32) \]
expresses the relationship between Fahrenheit temperature, \(F\),
and Celsius temperature, \(C\).  Use the formula to convert the
given Fahrenheit temperature to its equivalent temperature in 
the Celsius scale. (Enter your answer rounded to two decimal places and do not 
forget to include the units: For \(100^\circ C\) type, $BITALIC
100 degC $EITALIC.)
$PAR
\( $F^\circ \) F =  \{ ans_rule(20) \} 
END_TEXT

######################################
#  Answers

Context(LimitedNumeric);

$ans=Compute($C->substitute(F=>$F))->reduce;
ANS(NumberWithUnits("$ans degC")->cmp);

$showPartialCorrectAnswers = 1;

######################################


COMMENT('MathObject version');
ENDDOCUMENT();
