##DESCRIPTION
## DBsubject('Calculus')
## DBchapter('Limits')
## DBsection('')
## Date('6/6/2012')
## Author('Nandor Sieben')
## Institution('NAU')
## KEYWORDS('limit point','accumulation point')
##ENDDESCRIPTION


DOCUMENT();

loadMacros(
"PGstandard.pl",
"MathObjects.pl",
"PGcourse.pl",
);

TEXT(beginproblem());

Context("Interval");

$a = random(-10,10,1);
$b = $a+random(1,3,1);
$c = $b+random(1,3,1);
$d = $c+random(1,3,1);

$ansa = Compute("[$a,$c]");
$ansb = Compute("[$a,$b] U [$c,INF)");

BEGIN_TEXT
Find the set of limit points of the sets. Give your answer using interval notation. Use INF for infinity and U for union if needed.

$BR
$BR 

a. 
The set of limit points of the set \( [$a,$b) \cup ($b,$c) \cup $LBRACE $d $RBRACE \) is \{ans_rule(20)\}

$BR
$BR

b.
The set of limit points of the set \( [$a,$b) \cup ($c,\infty) \) is \{ans_rule(20)\}


END_TEXT

ANS( $ansa->cmp() );

ANS( $ansb->cmp() );

ENDDOCUMENT();


