##DESCRIPTION
#
# File Created: 6/5/2000
# Last Modified: 6/5/2000
# Problem Author: Joseph Neisendorfer
# WeBWorK Entry: David Etlinger
# Location: University of Rochester
#
# Asks for the partial derivatives of a
# field's scalar components, the line
# integral of a curve in that field, and
# whether the field is conservative
#
##ENDDESCRIPTION

##KEYWORDS('Multivariable','Vector','Partial','Line Integral','Conservative','Field')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGauxiliaryFunctions.pl");

TEXT( &beginproblem );
#Do Show Partial Credit
$showPartialCorrectAnswers = 1;

$ans1 = "(y^2 - x^2) / (x^2 + y^2)^2";
$ans2 = $ans1;

BEGIN_TEXT
Let \( \mathbf{F} ( x, y ) = \frac{-y \mathbf{i} + x \mathbf{j}}{x^{2} +
y^{2}} \) and let \(C\) be the circle 
\( \mathbf{r} \left( t \right) = \left( \cos t
\right) \mathbf{i} + \left( \sin t \right) \mathbf{j} \), \( 0 \leq t \leq 2\pi \).
$BR$BR
A. Compute \( \frac{\partial Q}{\partial x} \) $BR
\{ans_rule(40)\} $BR
 Note: Your answer should be an expression of \(x\) and \(y\); e.g. "3xy - y"
$BR$BR
B. Compute \( \frac{\partial P}{\partial y} \) $BR
\{ans_rule(40)\} $BR
 Note: Your answer should be an expression of \(x\) and \(y\); e.g. "3xy - y"
$BR$BR
C. Compute \( \int_C \mathbf{F} \cdot d\mathbf{r} \) $BR
\{ans_rule(40)\} $BR
 Note: Your answer should be a number
$BR$BR
D. Is $BBOLD F $EBOLD conservative in the whole \(xy\)-plane?
\{ pop_up_list('?' => '?', 'y' => 'Yes', 'n' => 'No' ) \}
END_TEXT

ANS( multivar_function_cmp( $ans1, 2 ) );
ANS( multivar_function_cmp( $ans1, 2 ) );
ANS( num_cmp( "2*pi" ) );
ANS( str_cmp( 'N' ) );

ENDDOCUMENT();        # This should be the last executable line in the problem.
