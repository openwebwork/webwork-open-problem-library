# DESCRIPTION
# Problem from Calculus, single variable, Hughes-Hallett et al., 4th ed.
# WeBWorK problem written by Gavin LaRose, <glarose@umich.edu>
# ENDDESCRIPTION

##KEYWORDS('Calculus', 'Arc Length')
##Tagged by jjh2b

##DBsubject('Calculus')
##DBchapter('Applications of Integration')
##DBsection('Arc Length')
##Date('')
##Author('LaRose')
##Institution('Michigan')
##TitleText1('Calculus')
##EditionText1('4')
##AuthorText1('Hughes-Hallett')
##Section1('8.2')
##Problem1('')

DOCUMENT();
loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl",
);
 
TEXT(&beginproblem);
$showPartialCorrectAnswers = 1;

$r = random(2,4,1);
$r1 = $r - 1;
$r1d = ( $r1 == 1 ) ? '' : $r1;
# we'll use one of these functions
@f = ( "t^{$r}",   "e^{$r t}", "\sin($r t)", "\cos($r t)",
       "\sqrt{t}", "5 - $r t", );
@d = ( "$r t^{$r1d}", "$r e^{$r t}", "$r\cos($r t)", "-$r\sin($r t)",
       "{1\over 2\sqrt{t}}", "-$r" );
# the value of the integral is then
@v = ( 1, "e^($r) - 1", "sin($r)", "cos($r) - 1", 1, "$r" );
@vtex = ( 1, "e - 1", "\sin($r)", "\cos($r) - 1", 1, "$r" );
@lft = ( 0, 1, 0, 1, 0, 0 );
@rgt = ( 1, "e", "\sin($r)", "\cos($r)", 1, "-$r" );
$whichf = random(0,(scalar(@f) - 1),1);

$ft = $f[$whichf];
$dt = $d[$whichf];
$x0 = $lft[$whichf];
$x1 = $rgt[$whichf];

BEGIN_TEXT

Find the length of the parametric curve
\( x = \cos($ft) \), \( y = \sin($ft) \) for 
\( 0 \leq t \leq 1 \). 
(Be sure you can explain why your answer is reasonable).

$PAR
arc length = \{ ans_rule(35) \}

END_TEXT

ANS( num_cmp( $v[$whichf] ) );

SOLUTION(EV3(<<'END_SOLUTION'));
$PAR SOLUTION $PAR

Note that \(\left({dx\over dt}\right)^2 = ($dt)^2 \sin^2($ft)\) and 
\(\left({dy\over dt}\right) = ($dt)^2 \cos^2($ft)\).  Therefore, the
arc length \(D\) along the curve is 
\[
  D = \int_{0}^{1} \sqrt{\left($dt\right)^2(\sin^2($ft) + \cos^2($ft))}\, dt 
\]
\[
    = \int_{0}^{1} \sqrt{\left($dt\right)^2}\,dt = \int_{0}^{1}\,$dt\,dt = 
    $ft\bigg|_0^1 = $vtex[$whichf].
\]
This is the length of the arc of a unit circle from the point 
\((\cos($x0), \sin($x0))\) to \((\cos($x1), \sin($x1))\), that is, 
between the angles \(\theta = $x0\) and \(\theta = $x1\).  The length
of this arc is \($vtex[$whichf]\).

END_SOLUTION

ENDDOCUMENT();
