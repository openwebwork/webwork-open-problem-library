# DESCRIPTION
# Problem from Calculus, single variable, Hughes-Hallett et al., 4th ed.
# WeBWorK problem written by Aaron Wooton, <wootton@up.edu>
# ENDDESCRIPTION

DOCUMENT();

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

$a=random(-8, 0, 2);
$b=random(0, 5);
$c=random(1, 9, 2);
$d=random(2, 9);
$a1=-$a;
$slope1=($b-$d);
$slope2=($a-$c);
$ans = "(($b-$d)/($a-$c))*(x+$a1 ) +$b";


BEGIN_TEXT

Find the equation of the line that passes through the points 
\( ($a,$b ) \) and \( ($c,$d ) \). 
$BR 
\(y=\)\{ans_rule( 60) \}

END_TEXT

ANS(function_cmp($ans));

# formatting
if ( $slope1 < 0 && $slope2 < 0 ) {
    $m = "\frac{" . (-1*$slope1) . "}{" . (-1*$slope2) . "}";
} elsif ( $slope1 < 0 || $slope2 < 0 ) {
    $m = "-\frac{" . abs($slope) . "}{" . abs($slope2) . "}";
} else {
    $m = "\frac{$slope}{$slope2}";
}

SOLUTION(EV3(<<'END_SOLUTION'));
$PAR SOLUTION $PAR

The slope is \( ($b-$d)/($a-$c))=($slope1/$slope2)=$m \). Using the
point \( ($a,$b ) \), the equation of the line is 
\( y- $b= $m (x-$a) \) or 
\( y= $m (x-$a) +$b \).
Note that we get the same result if we use the point \(($c,$d)\) (you
can check this by multiplying out the equation for the line that we
found here and comparing it with the result that you get using the
other point).

END_SOLUTION

ENDDOCUMENT();
