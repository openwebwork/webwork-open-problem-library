# DESCRIPTION
# Problem from Calculus, single variable, Hughes-Hallett et al., 4th ed.
# WeBWorK problem written by Aaron Wooton, <wootton@up.edu>
# ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

$a=random(20, 70, 2);
$b = random(($a+20),150,2);
$m=($b-$a)/80;
$m1=100*$m;
$c=$a-$m1;
$ans1 = "$m (w-100 ) + $a";

$slopeunits = new_multiple_choice();
$slopeunits->qa('Think about the interpretation of the slope: are the ' .
		'units of the slope', 'dollars per kilogram');
$slopeunits->extra('dollars', 'kilograms', 'kilograms per dollar');

$interunits = new_multiple_choice();
$interunits->qa('Think about the interpretation of the intercept: are the ' .
		'units of the intercept', 'dollars');
$interunits->extra('kilograms', 'dollars per kilogram', 
		   'kilograms per dollar');


BEGIN_TEXT

The monthly charge for a waste collection service is $a dollars
for 100 kg of waste and $b dollars for 180 kg of waste. 

$PAR

${BBOLD}(a)$EBOLD 
Find a linear model for the cost, \(C\), of waste collection as a
function of the number of kilograms, \(w\). 

$PAR

\( C= \) \{ans_rule( 60) \}

$PAR
${BBOLD}(b)$EBOLD 
What is the slope of the line found in part (a)? 

$PAR
Slope= \{ans_rule( 60) \}

$PAR
\{ $slopeunits->print_q() \}
$BR
\{ $slopeunits->print_a() \}

$PAR
${BBOLD}(c)$EBOLD 
What is the value of the vertical intercept of the line found in
part (a)?  

$PAR
Value= \{ans_rule( 60) \}

$PAR
\{ $interunits->print_q() \}
$BR
\{ $interunits->print_a() \}

END_TEXT

ANS(function_cmp($ans1,"w"));
ANS(num_cmp($m));
ANS(radio_cmp( $slopeunits->correct_ans() ));
ANS(num_cmp($c));
ANS(radio_cmp( $interunits->correct_ans() ));

SOLUTION(EV3(<<'END_SOLUTION'));
$PAR SOLUTION $PAR
${BBOLD}(a)$EBOLD 
We find the slope \(m\) and intercept \(b\) in the linear equation
\(C=b+mw\). To find the slope \(m\), we use 

$PAR
\(m=\frac{\Delta C}{\Delta w} =\frac{$b-$a}{180-100}=$m\).

$PAR
We substitute to find b:
$PAR
\($a=b+($m)(100)\)
$PAR
\(b=$a-$m1=$c\).

$PAR
The linear formula is \(C=$m w+$c\).

$PAR
${BBOLD}(b)$EBOLD 
The slope is \($m\) dollars per kilogram. Each additional kilogram
of waste costs \($m1\) cents. 

$PAR
${BBOLD}(c)$EBOLD 
The intercept is \($c\) dollars. The flat monthly fee to subscribe
to the waste collection is \($c\) dollars, even if there is no waste.

END_SOLUTION


ENDDOCUMENT();        # This should be the last executable line in the problem.
