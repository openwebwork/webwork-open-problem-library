##DESCRIPTION
##KEYWORDS('integrals', 'area between curves')
##This is setIntegrals19Area/ns6_1_25.pg
##slightly cleaned up by Zig Fiedorowicz Jan. 2000
##ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
  "PG.pl",
  "PGbasicmacros.pl",
  "PGchoicemacros.pl",
  "PGanswermacros.pl",
  "PGauxiliaryFunctions.pl",
  "PGunion.pl",
  "PGcourse.pl",
);

TEXT(beginproblem());
BEGIN_PROBLEM();

######################################################################
# Setup

$f = random(2,4,1);
$g = random(2,4,1);
$c = arctan($g/$f);
$c1 = spf($c/$PI,"%4.1f")+.1;
$a = random($c1,1,.1);
$b = $a*$PI;

$ans = 2*$f*cos($c) + 2*$g*sin($c) - $f*cos($b) - $g*sin($b)-$f;

######################################################################
#  Main text

BEGIN_TEXT

Find the area of the region enclosed between \(y=$f \sin(x)\) and \(y=$g \cos(x)\) from
\(x=0\) to \(x=$a\pi\).
$PAR
The area is \{ans_rule(45)\}.
$PAR
$BITALIC
Hint: Notice that this region consists of two parts.
$EITALIC
END_TEXT

##set $PG_environment{'textbook'} in webworkCourse.ph
if (defined($textbook)) {
   if ($textbook eq "EllisGulick5") {
BEGIN_TEXT
This is similar to Example 1 in Section 5.8 of the text.
END_TEXT
   }
}


######################################################################
# Answers

ANS(num_cmp($ans));
$showPartialCorrectAnswers = 1;

######################################################################

END_PROBLEM();
ENDDOCUMENT();        # This should be the last executable line in the problem.
