##DESCRIPTION
##KEYWORDS('integrals', 'theory', 'definite', 'area')
##  Find integral by interpreting as area
##  Authored by Zig Fiedorowicz 5/12/2001, 
##     edited by K. Lesh, Union College, 10/4/03
##ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
   "PG.pl",
   "PGbasicmacros.pl",
   "PGchoicemacros.pl",
   "PGanswermacros.pl",
   "PGauxiliaryFunctions.pl",
   "PGunion.pl",        # Union College utilities
   "PGcourse.pl",       # Customization file for the course
);

TEXT(beginproblem());
BEGIN_PROBLEM();

$x[0] = random(-8,8,1);
$y[0] = random(-1,1,2)*random(1,8,1);

$area = 0;

for ($i=1; $i<4; $i++) {
    $x[$i] = $x[$i-1] + random(2,5,1);
    $y[$i] = random(1,8,1);
    if ($y[$i-1]>0) {$y[$i] = - $y[$i];}
    $area = $area + ($y[$i-1]+$y[$i])*($x[$i]-$x[$i-1])/2;
}

BEGIN_TEXT
You are given the four points in the plane \(A = ($x[0],$y[0])\), \ 
\(B = ($x[1],$y[1])\), \ \(C = ($x[2],$y[2])\), and \(D = ($x[3],$y[3])\).
The graph of the function \(f(x)\) consists of the three line segments
\(AB\), \(BC\) and \(CD\).  
$PAR
Evaluate the integral \( \displaystyle \int_{$x[0]}^{$x[3]} f(x)\,dx\)
by interpreting it in terms of signed areas.
In other words, draw a picture of the region the integral
represents, and find the area using high school geometry.
$PAR
\( \displaystyle \int_{$x[0]}^{$x[3]} f(x)\,dx =\)\{ans_rule(20)\}
END_TEXT

$showPartialCorrectAnswers = 1;
ANS(num_cmp($area));

END_PROBLEM();
ENDDOCUMENT();

