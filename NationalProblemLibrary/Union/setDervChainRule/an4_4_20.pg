##DESCRIPTION
##ENDDESCRIPTION
## Edited Z. Song, Union College, 9/7/03

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
  "PG.pl",
  "PGbasicmacros.pl",
  "PGchoicemacros.pl",
  "PGanswermacros.pl",
  "PGauxiliaryFunctions.pl",
  "PGunion.pl",        # Union College utilities
  "PGcourse.pl",       # Customization file for the course
);

TEXT(beginproblem());
BEGIN_PROBLEM();

$a = non_zero_random(-10,10,2);

$b = random(2,6,1);
$c = non_zero_random(-10,10,2);
$expb=$b-1;
$d = $a*$b*$c;
$derv = "${d}e^(${a}x^$b)(x^$expb)";

BEGIN_TEXT
Suppose that \( f(x) = ${c}e^{${a}x^{$b}}\).
Find \(f'(x)\).
$PAR
\(f'(x)\) = \{ans_rule(60)\}.
END_TEXT

$showPartialCorrectAnswers = 1;

ANS(fun_cmp(
  $derv,                 #  the correct answer
#  limits => [0,1],       #  range of x's to use
#  relTol => .1,          # .1 percent, (i.e., .001)
));

##############################################

END_PROBLEM();
ENDDOCUMENT();        # This should be the last executable line in the problem.
