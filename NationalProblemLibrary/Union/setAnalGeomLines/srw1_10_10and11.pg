##DESCRIPTION
##  find the equation of the line given slope and a point on the line
##ENDDESCRIPTION
## Edited K. Lesh, Union College, 7/19/03
## Update to new parser and combine two problems 9/7/05, KL

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
   "PGstandard.pl",
   "PGunion.pl",                # Union College utilities
   "Parser.pl",
   "PGcourse.pl",               # Customization file for the course
);

TEXT(beginproblem());

BEGIN_PROBLEM();
#first problem
$x1 = random(-9,-2,1);
$y1 = random(-7,-2,1);
$m1 = random(-5,-1,1);

#second problem
$a1 = random(2,5,1);
$b1 = random(1,10,1);
$a2 = random(6,10,1);
$b2 = random(1,10,1);
while ($b1==$b2) {$b2  = random(1,10,1)}

$M1 = ($b2-$b1)/($a2-$a1);


BEGIN_TEXT
\{BeginList("OL",type=>"a")\}
$ITEM
The equation of the line with slope \($m1 \) that goes through the point
\( ( $x1 ,$y1 ) \) can be written in the form \( y = mx+b \) where 
$BR
\( m = \) \{ans_rule(15)\}
$BR
 \( b = \)  \{ans_rule(15)\}
$ITEMSEP
$ITEM
The equation of the line that goes through the 
points \( ( $a1 ,$b1 ) \) and \( ( $a2 ,$b2 ) \)
can be written in the form \( y = mx+b \) where 
$BR
\( m = \) \{ans_rule(15)\}
$BR
\( b = \)  \{ans_rule(15)\}
\{EndList()\}
END_TEXT

$showPartialCorrectAnswers = 1;

$ans1 = $m1;
$ans2 = $y1 -$m1*$x1;
ANS(Real($ans1)->cmp);
ANS(Real($ans2)->cmp);


$ans3 = $M1;
$ans4 = $b1 -$M1*$a1;
ANS(Real($ans3)->cmp);
ANS(Real($ans4)->cmp);

END_PROBLEM();
ENDDOCUMENT();        # This should be the last executable line in the problem.

