##DESCRIPTION
##Problem 13, section 2.3 Fundamentals of Complex Analysis for Mathematics,
##Science, and Engineering by E.B. Saff and A.D. Snider
##ENDDESCRIPTION

##KEYWORDS('true-false')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 0;

@questions =( "\(f(z)^3\) is entire.",
	"\(f(z)g(z)\) is entire.",
	"\(f(z)/g(z)\) is entire.",
	"\(5f(z)\ +\ ig(z)\) is entire.",
	"\(f(1/z)\) is entire.",
	"\(g(z^2\ +\ 2)\) is entire.",
	"\(f(g(z))\) is entire.",
);
@answers =( 'T', 'T', 'F', 'T', 'F', 'T', 'T');

@slice = NchooseK(scalar(@questions), 7);

BEGIN_TEXT
Let \(f(z)\) and \(g(z)\) be entire functions. Decide which of the following
statements are always true. $PAR
Answer T or F:$PAR
END_TEXT

## Next we output the 4 chosen questions.  #match_
TEXT(
&match_questions_list(@questions[@slice])
);

ANS(str_cmp([ @answers[@slice] ] ));
ENDDOCUMENT();        # This should be the last executable line in the problem.
