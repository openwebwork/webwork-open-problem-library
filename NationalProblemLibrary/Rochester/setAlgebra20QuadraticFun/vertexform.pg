## DESCRIPTION
## Algebra
## ENDDESCRIPTION

## KEYWORDS('quadratic', 'function' 'limits' 'intercepts')
## Tagged by tda2d

## DBsubject('Algebra')
## DBchapter('Functions')
## DBsection('Extreme Values of Functions')
## Date('6/3/2002')
## Author('')
## Institution('')
## TitleText1('College Algebra')
## EditionText1('3')
## AuthorText1('Stewart, Redlin, Watson')
## Section1('4.6')
## Problem1('28')


DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGanswermacros.pl",
"Parser.pl"
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

$A = non_zero_random(-10,10);
$H = non_zero_random(-29,29);
$K = random(1,50);

$minmax = "Maximum";
if ($A > 0) {$K = -$K; $minmax = "Minimum";}

$vertex = "($H,$K)";
$smallroot = $H - sqrt(-$K/$A);
$bigroot = $H + sqrt(-$K/$A);

$extreme = $K;

$func = nicestring([$A, $K], ["(x-$H)^2", '']);

BEGIN_TEXT;
Given the function \(f(x) = $func\) find all of the following:
$BR$BR
The vertex of the function is \{ans_rule(15)\}.
$BR$BR
Does the function have a minimum or a maximum? 
 \{pop_up_list(['?','Minimum', 'Maximum'])\} $BR$BR
Find the extreme value of the function. \{ans_rule(15)\} $BR$BR
The smallest root is \{ans_rule(15)\}.$BR$BR
The largest root is \{ans_rule(15)\}.
END_TEXT;

Context("Point");

ANS(Point("($H,$K)")->cmp);
ANS(std_cs_str_cmp($minmax));
ANS(num_cmp($extreme));
ANS(num_cmp($smallroot));
ANS(num_cmp($bigroot));

ENDDOCUMENT();        # This should be the last executable line in the problem.

