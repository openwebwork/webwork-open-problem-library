## DESCRIPTION
## Algebra: Basic Algebra: Solving complex fractions
## ENDDESCRIPTION

## KEYWORDS ('algebra', 'fractions', 'complex fractions')
## Tagged by cmd6a 4/4/06

## DBsubject('Algebra')
## DBchapter('Basic Algebra')
## DBsection('Exponents and Radicals')
## Date('2001/2002')
## Author('Mark Schmitt')
## Institution('Rochester')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

$s = random(11,32,1);
$t = random(23,93,1);
$u = random(84,102,1);


TEXT(EV2(<<EOT));
The total resistance, R, of a particular group is given by the formula:
\[R = S + \left(\frac{1}{\frac{1}{T}+\frac{1}{V}}\right) \]
$BR
$BR
This formula can be simplified to the form \(\frac{A}{B}\) where A and B contain no fractions.
$BR
A = \{ans_rule(10)\}
$BR
B = \{ans_rule(10)\}
$BR
$BR
$BR
Suppose that \( S = $s \Omega , T = $t \Omega \rm{and} V = $u \Omega \).
$BR
Then R = \{ans_rule(10)\} Note: Your answer must be a decimal.

EOT

$ans1 = "T * V + S*(T+V)";
$ans2 = "T+V";
$ans3 = ($t *$u + $s*($t+$u))/($t+$u);
ANS(fun_cmp($ans1,var=>['S','T','V']));
ANS(fun_cmp($ans2,var=>['S','T','V']));
ANS(num_cmp($ans3, mode=>"strict"));
ENDDOCUMENT();        # This should be the last executable line in the problem.

