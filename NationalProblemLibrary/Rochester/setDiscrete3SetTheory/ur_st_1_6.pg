##DESCRIPTION
##KEYWORDS('Standard Example')
##This problem is also in the directory setSetTheory1
##Question 12, Section 1.4 from Discrete Math by Kenneth Rosen
##ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(beginproblem());

#Numerical Answer

BEGIN_TEXT
What is the cardinality of each of the following sets? $PAR
(a) \( \emptyset \) $BR
\{ ans_rule(10) \}
$PAR
(b) \( \lbrace \emptyset \rbrace \) $BR
\{ ans_rule(10) \}
$PAR
(c) \( \lbrace \emptyset, \lbrace \emptyset \rbrace \rbrace \) $BR
\{ ans_rule(10) \}
$PAR
(d) \( \lbrace \emptyset, \lbrace \emptyset \rbrace, \lbrace \emptyset,
\lbrace \emptyset \rbrace \rbrace \rbrace \) $BR 
\{ ans_rule(10)\}
$PAR
END_TEXT

ANS(num_cmp(0) );
ANS(num_cmp(1) );
ANS(num_cmp(2) );
ANS(num_cmp(3) );

ENDDOCUMENT();        # This should be the last executable line in the problem.;
