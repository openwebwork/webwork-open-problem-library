##DESCRIPTION
##KEYWORDS('linear', 'transformation', 'matrix', 'upper triangular', 'basis')
##
##ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGgraphmacros.pl",
"PGmatrixmacros.pl", 
"PGnumericalmacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

@c = NchooseK(9,9);
foreach $i (0..8) { 
	$c[$i] = $c[$i]+1;
}

$RIGHT_BRACE = '\}';

BEGIN_TEXT

Find the matrix \(A\) of the linear transformation $BR
\( T(M)= \{display_matrix_mm([[$c[0], $c[1]], [0, $c[2]]])\} M \) 
$BR
from \(U^{2\times 2}\) to \(U^{2\times 2}\), with respect to the basis 
$BR
\( \left\{"\{"\} \{display_matrix_mm([[1, 0], [0, 0]])\} , \{display_matrix_mm([[1, 1], [0, 0]])\} , 
	         \{display_matrix_mm([[0, 0], [0, 1]])\} \right${RIGHT_BRACE} \).
$BR
\{ mbox( '\(A=\)', answer_matrix(3,3,5) ) \}

END_TEXT

ANS(num_cmp($c[0]));
ANS(num_cmp(0));
ANS(num_cmp(- $c[1]));
ANS(num_cmp(0));
ANS(num_cmp($c[0]));
ANS(num_cmp($c[1]));
ANS(num_cmp(0));
ANS(num_cmp(0));
ANS(num_cmp($c[2]));


ENDDOCUMENT();       # This should be the last executable line in the problem.

