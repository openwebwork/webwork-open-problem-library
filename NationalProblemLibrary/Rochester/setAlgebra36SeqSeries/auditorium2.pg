#DESCRIPTION
#Sequences
#ENDDESCRIPTION

#KEYWORDS('Sequences')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl"
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 0;

$rows = random(20,80,1);
$first = random(30,100,10);
$more = random(2,7,1);
$n = random (4,10,1);

$seats = $n*(2*$first+($n-1)*$more)/2;
$total = $rows*(2*$first+($rows-1)*$more)/2;


BEGIN_TEXT;

In a certain auditorium, each row has $more more seats than the row in front of
it.  The first $n rows contain $seats seats.  How many rows does the 
auditorium have if it holds $total seats?

$BR
$BR
The auditorium has \{ans_rule(10)\} rows.

END_TEXT
ANS(num_cmp($rows));

ENDDOCUMENT();        # This should be the last executable line in the problem.


