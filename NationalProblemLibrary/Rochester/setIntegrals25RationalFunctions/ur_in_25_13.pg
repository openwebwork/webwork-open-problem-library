## DESCRIPTION
## Calculus
## ENDDESCRIPTION

## KEYWORDS('integral' 'partial fraction')
## Tagged by tda2d

## DBsubject('Calculus')
## DBchapter('Techniques of Integration')
## DBsection('Integration by Partial Fractions')
## Date('')
## Author('Paul Pearson')
## Institution('Rochester')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')
## TitleText2('Calculus: Early Transcendentals')
## EditionText2('1')
## AuthorText2('Rogawski')
## Section2('7.6')
## Problem2('9')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl",
"MathObjects.pl",
"parserFormulaUpToConstant.pl",
"PGcourse.pl",
"answerHints.pl",
);

TEXT(beginproblem());
$showPartialCorrectAnswers = 1;

Context("Numeric");
Context()->variables->set(x=>{limits=>[-12,-6]});

$a=random(-4,-1,1);
$b=random(1,4,1);
while($a==-$b) {$b=random(1,4,1)};

BEGIN_TEXT
Evaluate the indefinite integral.
$BR
$BR \( \displaystyle \int \frac{1}{(x+$a)(x+$b)} \, dx = \) \{ans_rule(50) \} 
END_TEXT

$d1= $b-$a;
$soln = FormulaUpToConstant("-ln(abs(x+$b)) / $d1 + ln(abs(x+$a)) / $d1")->reduce;

ANS( $soln->cmp() );


#sub mycheck {
#  my ($correct, $student, $ansHash) = @_;
#  if ($student == FormulaUpToConstant("x")) {
#      Value::Error("Too many arguments");
##      $ansHash->setMessage(1,"Check your limits of integration.");
#      return 0;
#  } else {
#      return $student == $soln;
#  }
#}

#ANS( $soln->cmp( checker=>~~&mycheck ) );

#ANS($soln->cmp->withPostFilter(sub { 
#  my $ans = shift; 
#  $ans->{ans_message} = "Don't forget absolute value" 
#  if $ans==FormulaUpToConstant("-ln(x+$b) / $d1 + ln(x+$a) / $d1"); 
#  return $ans; 
#}));


#ANS( $soln->cmp()
#-> withPostFilter(AnswerHints(
#FormulaUpToConstant("-ln(x+$b) / $d1 + ln(x+$a) / $d1") => "Don't forget absolute value",
#FormulaUpToConstant("0") => ["Don't guess", checkTypes => 0, replaceMessage => 1]
#))
#);


#ANS( $soln->cmp() );

#ANS(fun_cmp($soln, limits=>[6,12], mode=>"antider", vars=>'x'));

ENDDOCUMENT();        # This should be the last executable line in the problem.
