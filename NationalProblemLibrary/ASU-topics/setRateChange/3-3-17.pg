## DESCRIPTION
## Derivatives
## ENDDESCRIPTION

## KEYWORDS('Derivatives' 'Rates of Change')
## Tagged by tda2d

## DBsubject('Calculus')
## DBchapter('Limits and Derivatives')
## DBsection('Tangents, Velocities, and Other Rates of Change')
## Date('')
## Author('')
## Institution('ASU')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')


DOCUMENT();        # This should be the first executable line in the problem.

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGauxiliaryFunctions.pl");


$a = non_zero_random(-8,8,1);
while ($a**2 == 1)
{
    $a = non_zero_random(-8,8,1);
}
$b = random(1,3,1);
$c = random(7,9,1);
$d = random(4,6,1);

TEXT(beginproblem());

TEXT(EV2(<<EOT));
Let \( f(x) = x^2 ? {$a} x\).
$BR
$BR
(A) Find the slope of the secant line joining
\(($b, f($b))\) and \(($c, f($c))\).
$PAR
Slope of secant line =  \{ans_rule(20)\}
$BR
$BR
(B) Find the slope of the secant line joining
\(($d, f($d))\) and \(($d + h,  f($d + h))\).
$PAR
Slope of secant line =  \{ans_rule(20)\}
$BR
$BR
(C) Find the slope of the tangent line at
\(($d, f($d))\).
$PAR
Slope of tangent line =  \{ans_rule(20)\}
$BR
$BR
(D) Find the equation of the tangent line at
\(($d, f($d))\).
$PAR
\(y = \)  \{ans_rule(20)\}

EOT
$ans1 = (($c**2 + $a*$c)-($b**2 + $a*$b))/($c-$b);
$ans2 = "(2*$d+$a)*(x-$d)+($d**2+$a*$d)";
@answers = (std_num_cmp($ans1), function_cmp("2*$d + h + $a","h"),
std_num_cmp(2*$d + $a), function_cmp($ans2,"x"));

ANS( @answers );

ENDDOCUMENT();        # This should be the last executable line in the problem.
