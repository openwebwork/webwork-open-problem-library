## DESCRIPTION
## Algebra and Number Theory: Algebra
## ENDDESCRIPTION

## KEYWORDS('algebra', 'number', 'theory')
## Tagged by YJ

## DBsubject('Algebra and Number Theory')
## DBchapter('Algebra')
## DBsection('Graphing')
## Date('5/26/2005')
## Author('Jeff Holt')
## Institution('UVA')
## TitleText1('Calculus')
## EditionText1('5e')
## AuthorText1('Stewart')
## Section1('3.1.1')
## Problem1(' ')

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl",
"PGgraphmacros.pl",
"PGasu.pl",
"extraAnswerEvaluators.pl"
);

TEXT(&beginproblem);

$a=non_zero_random(-6,6);
$b=non_zero_random(-6,6);
$a1 = -1+$a;
$b1 = 2+$a;
$c1 = 5+$a;
$a2 = -$b;
$b2 = -1-$b;
$c2 = -$b;


BEGIN_TEXT
The graph of \( y= f(x) \) is given below: 
(to get a better look at the graph, you can click on it).$BR
\{ image("graph.gif")\}
$PAR
On a piece of paper sketch the graph of \( y=f(x-$a) \) and
determine the new coordinates of points A, B and C.

$PAR
     A = \{ans_rule(10)\}
$PAR
     B = \{ans_rule(10)\}
$PAR
     C = \{ans_rule(10)\}

$PAR
On a piece of paper sketch the graph of \( y=-f(x)-$b \) and
determine the new coordinates of points A, B and C.

$PAR
     A = \{ans_rule(10)\}
$PAR
     B = \{ans_rule(10)\}
$PAR
     C = \{ans_rule(10)\}
END_TEXT


&ANS(interval_cmp( "($a1, 0)" ) );
&ANS(interval_cmp( "($b1, 1)" ) );
&ANS(interval_cmp( "($c1, 0)" ) );
&ANS(interval_cmp( "(-1, $a2)" ) );
&ANS(interval_cmp( "(2, $b2)" ) );
&ANS(interval_cmp( "(5, $c2)" ) );

ENDDOCUMENT();        # This should be the last executable line in the problem.

