#DESCRIPTION
#KEYWORDS('maximum,minimum', 'increase,decrease', 'convex')
# Graphing -- intervals of increase and decrease, convexity
# f(x) = ( x^3 + 12x^2 + 48x + 64 ) / ( x^2 + 8x - 20 )
#ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
"PGauxiliaryFunctions.pl");

sub ff {
    my $in= shift @_;
    $in*$in;
    }

TEXT(&beginproblem);
$showPartialCorrectAnswers=1;

$offset = random(-4,4,1);
$a=random(1,6,1);
$asquare=$a*$a;
$const = -$asquare + $offset**2;
$b = -2*$offset;

#numerator calculations
$c1=-3*$offset;
$c2=3*$offset**2;
$c3=-$offset**3;

$left = random(-20,-15,1) +$offset;
$right = random(15,20,1) +$offset;

# strange bug
TEXT(EV2(<<EOT));
Answer the following questions for the function \[
f(x) = \frac{ x^3 + $c1 x^2 + $c2 x + $c3  }{ x^2 + $b x + $const  }
\]
defined on the interval \( [  $left, $right ]\).
$PAR
Enter points, such as inflection valuess in ascending order, i.e. smallest x values first.
EOT

TEXT(OL(
EV2(" The function \( f(x) \) has vertical asympototes at \{&ans_rule(10)\}
and \{&ans_rule(10)\}"),
EV2( "\( f(x)     \) is concave down on the region \{&ans_rule(10)\}
to \{&ans_rule(10)\} and \{&ans_rule(10)\} to \{&ans_rule(10)\} ")
));
&ANS(std_num_cmp_list(.1, "", -$a+$offset, $a+$offset)); #asymptotes
&ANS(std_num_cmp_list(.1, "", $left, -$a+$offset,$offset,$a+$offset)); #concave down

ENDDOCUMENT();        # This should be the last executable line in the problem.
