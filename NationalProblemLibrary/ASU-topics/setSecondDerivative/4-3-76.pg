#DESCRIPTION
#KEYWORDS('algebara', 'equations', 'domain')
#  Find the domain of a function root(a-bx)
#ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGauxiliaryFunctions.pl");

$a = random(2,8,1);
$b = random(2,8,1);

TEXT(beginproblem());

$showPartialCorrectAnswers = 1;

TEXT(EV2(<<EOT));
Suppose that
\[ f(x) = \frac{x^2}{$a x^2 ? {$b}}.  \]
$BR
$BR
(A) Find the \textbf{smallest} inflection point of \(f\).
(If there are no inflection points, enter -1000.)
$PAR
Smallest inflection point = \{ans_rule(20)\}
$BR
$BR
(B) Find the \textbf{largest} inflection point of \(f\).
(If there are no inflection points, enter -1000.)
$PAR
Largest inflection point = \{ans_rule(20)\}

EOT

@answers = ( std_num_cmp(-sqrt($b/(3*$a))), std_num_cmp(sqrt($b/(3*$a))) );

ANS( @answers );

ENDDOCUMENT();        # This should be the last executable line in the problem.
