#DESCRIPTION
#KEYWORDS('algebara', 'equations', 'domain')
#  Find the domain of a function root(a-bx)
#ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros("PG.pl",
           "PGbasicmacros.pl",
           "PGchoicemacros.pl",
           "PGanswermacros.pl",
           "PGauxiliaryFunctions.pl");

$a = random(2,8,1);
$b = random(2,8,1);

TEXT(beginproblem());

$showPartialCorrectAnswers = 1;

TEXT(EV2(<<EOT));
Suppose that
\[ f(x) = \frac{x^3}{$a x^2 ? {$b}}.  \]
$BR
$BR
(A) Find the \textbf{average} of the \textbf{two smallest} inflection points of \(f\).
$PAR
Average of two smallest inflection points = \{ans_rule(20)\}
$BR
$BR
(B) Find the \textbf{average} of the \textbf{two largest} inflection points of \(f\).
$PAR
Average of two largest inflection points = \{ans_rule(20)\}

EOT

@answers = ( std_num_cmp(-sqrt(3*$b/($a))/2), std_num_cmp(sqrt(3*$b/($a))/2) );

ANS( @answers );

ENDDOCUMENT();        # This should be the last executable line in the problem.
