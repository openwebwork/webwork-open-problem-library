##DESCRIPTION
##KEYWORDS('limits','left,right,one-sided')
## tsuda tagged and PAID on 2-20-2004

## DBchapter('Limits and Derivatives')
## DBsection('Calculating Limits using the Limit Laws')
## Date('6/3/2002')
## Author('')
## Institution('')
## TitleText1('Calculus Early Transcendentals')
## EditionText1('4')
## AuthorText1('Stewart')
## Section1('2.3')
## Problem1('46')

## limits
##ENDDESCRIPTION

DOCUMENT();        # This should be the first executable line in the problem.

loadMacros("PG.pl", "PGbasicmacros.pl", "PGchoicemacros.pl",
"PGanswermacros.pl",
"PGauxiliaryFunctions.pl");

TEXT(&beginproblem);
$showPartialCorrectAnswers = 1;

$a=random(1, 5);
$b=non_zero_random(-5, 5);

$x1=random(1, 10);
if ($x1 == $b) { $x1 = $x1 + 1; }

$x2=random(-10, 0);

$a1 = $a + $x1;
$ab = $a + $b;

$c=$a1 - $x2;

TEXT(EV2(<<EOT));
Let \[ f(x) = \begin{cases}
$a1       & \text{ if } x \gt $x1 \\
$ab     & \text{ if } x =$x1 \\
-x+ $a1 & \text{ if } $x2 \le x \lt $x1 \\
$c         & \text{ if } x \lt $x2 
\end{cases} \]

Sketch the graph of this function and find following
limits if they exist (if not, enter DNE).
EOT

@limits =(); ##MAKE SURE WE START WITH AN EMPTY ARRAY
@answers =();     ##MAKE SURE WE START WITH AN EMPTY ARRAY

qa(~~@limits, ~~@answers,
"\( \displaystyle \lim_{ x \rightarrow $x1^{-}} f(x) \)", $a,
"\( \displaystyle \lim_{ x \rightarrow $x1^{+}} f(x) \)", $a1,
"\( \displaystyle \lim_{ x \rightarrow $x1} f(x) \)", "DNE",
"\( \displaystyle \lim_{ x \rightarrow $x2^{-}} f(x) \)", $c,
"\( \displaystyle \lim_{ x \rightarrow $x2^{+}} f(x) \)", $c,
"\( \displaystyle \lim_{ x \rightarrow $x2} f(x) \)", $c
);

TEXT(EV2(match_questions_list(@limits)));

&ANS(std_num_str_cmp($answers[0],[DNE]));
&ANS(std_num_str_cmp($answers[1],[DNE]));
&ANS(std_num_str_cmp($answers[2],[DNE]));
&ANS(std_num_str_cmp($answers[3],[DNE]));
&ANS(std_num_str_cmp($answers[4],[DNE]));
&ANS(std_num_str_cmp($answers[5],[DNE]));

ENDDOCUMENT();        # This should be the last executable line in the problem.

