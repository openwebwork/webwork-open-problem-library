##DESCRIPTION
##  Lines: Slope
##ENDDESCRIPTION


## BookTitle('Basic Algebra')
## Author1('David Covert')
## Author2('Teri Christiansen')
## DBsubject(Algebra)
## Date(Spring 2012)
## Institution(University of Missouri)
## Status(R)
## TitleText1('College Algebra')
## AuthorText1('Beecher, Penna, Bittinger')
## EditionText1('4')
## Section1('1.2')
## Problem1('')
## KEYWORDS('lines', 'slope')



DOCUMENT();        # This should be the first executable line in the problem.

loadMacros(
"PGstandard.pl",
"MathObjects.pl",
"contextFraction.pl",
"AnswerFormatHelp.pl",
"PeriodicRerandomization.pl",
"PGcourse.pl",
"PG.pl",
"PGbasicmacros.pl",
"PGchoicemacros.pl",
"PGanswermacros.pl",
);

TEXT(beginproblem());

$showPartialCorrectAnswers = 1;

$x1 = non_zero_random(-10,10,1);
$y1 = non_zero_random(-10,10,1);

#$case = random(1,3,1);
$case = 3;


Context("Fraction-NoDecimals");

if($case ==1){
   do {$x2 = random(-10,10,1)} until ($x1 != $x2);
   $y2 = random(-10,10,1);
   $m = Fraction($y1 - $y2,$x1 - $x2);
   ANS( Compute($m) -> cmp(studentsMustReduceFractions=>1) );
}
elsif($case ==2){
   do {$x2 = random(-10,10,1)} until ($x1 != $x2);
   $y2 = $y1;
   $m = 0;
   ANS( Compute($m) -> cmp() );
}

Context()->strings->add("Does Not Exist"=>{},"None"=>{alias=>"Does Not Exist"});

if($case ==3){
   do {$y2 = random(-10,10,1)} until ($y2 != $y1);
   $x2 = $x1;
   ANS( String("Does Not Exist")-> cmp() );
}





Context()->texStrings;



Context()->normalStrings;
BEGIN_TEXT
Find the slope of the line, if it exists, containing the points \(($x1,$y1)\) and \( ($x2,$y2)\).  If the slope does not exist, write "Does Not Exist" or "None".
$PAR
\(m = \) \{ans_rule(10)\}
END_TEXT


ENDDOCUMENT();        # This should be the last executable line in the problem.